// This file runs before all tests to set up the environment
import { beforeAll } from 'vitest';

// Setup any global configurations if needed
beforeAll(() => {
  // Global test setup can go here
});
