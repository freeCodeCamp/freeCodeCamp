Ensure when updating build, that after npm run build is run /*eslint-disable*/
 is added at the top of main js file in static folder or the build will cause eslint errors.

jquery and nicescroll are now redundant and can be removed on next build.