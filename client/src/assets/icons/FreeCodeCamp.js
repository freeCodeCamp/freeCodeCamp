import React, { Fragment } from 'react';

function FreeCodeCamp(props) {
  const altText="freeCodeCamp.org";
  return (
    <Fragment>
      <svg
        xmlns:xlink="https://s3.amazonaws.com/freecodecamp/freecodecamp_logo.svg">
      >
      <title>{altText}</title>
      </svg>
    </Fragment>
  );
}
export default FreeCodeCamp;
