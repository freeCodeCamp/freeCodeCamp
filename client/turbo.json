{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["FCC_*"],
      "outputs": ["public/**"]
    },
    "setup": {
      "env": ["FCC_*"],
      "outputs": [
        "config/env.json",
        "i18n/locales/*.json",
        "static/curriculum-data/**",
        "static/js/**"
      ]
    }
  }
}
