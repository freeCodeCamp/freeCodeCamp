/* const { parseMD } = require('./tools/challenge-parser/parser');

const parsed = parseMD('./step-009.md');

console.log('========== after parsed');

parsed.then(data => {
  console.log(data);
});*/
