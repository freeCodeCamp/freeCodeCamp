let xp = 0;
let health = 100;
let gold = 50;
let currentWeapon = 0;
let fighting;
let monsterHealth;
let inventory = ["stick"];

const button1 = document.querySelector("#button1");

// Here the ids of the other HTML elements that we want a reference to in the JavaScript code: button2, button3, text, xpText, healthText, goldText, monsterStats, monsterNameText, monsterHealthText. Just like you did with button1, create variables and set them equal to the element references.