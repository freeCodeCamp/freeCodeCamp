var xp = 0;

// To keep track of the players expereince points, we've declared a variable called xp and assigned it the starting value of 0. Create another variable to keep track of health and start it at 100.