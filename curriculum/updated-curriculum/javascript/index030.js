var xp = 0;
var health = 100;
var gold = 50;

// Create a variable to store the current weapon and set it to 0. When a name has two words, the convention is to use so-called "lower camel case". The variable name should look like this: currentWeapon.