id: 66f1a2009e65c9a40a26d51e
title: React Basics Quiz
challengeType: 8
dashedName: quiz-react-basics
---

# --description--

Answer all of the questions below correctly to pass the quiz.

# --quizzes--

## --quiz--

### --question--

#### --text--

What is the main purpose of `props` in React?

#### --distractors--

To store component state.

---

To handle events.

---

To manage the component's lifecycle.

#### --answer--

To pass data from parent to child components.

### --question--

#### --text--

Which hook is used to add state to a functional component in React?

#### --distractors--

`useRef()`

---

`useEffect()`

---

`useContext()`

#### --answer--

`useState()`

### --question--

#### --text--

What is the significance of the `key` prop when rendering lists in React?

#### --distractors--

It sets the display order of the list items.

---

It assigns a unique ID to each component.

---

It defines how the items will be sorted.

#### --answer--

It helps React identify which items have changed, are added, or are removed.

### --question--

#### --text--

What is JSX in React?

#### --distractors--

A library for creating components.

---

A JavaScript function for handling user input.

---

A file extension used for CSS in React.

#### --answer--

A syntax extension for JavaScript that looks similar to HTML.

### --question--

#### --text--

What does the `useEffect` hook do in React?

#### --distractors--

It manages component state.

---

It performs side effects in functional components.

---

It provides context to child components.

#### --answer--

It performs side effects in functional components.

### --question--

#### --text--

What is the purpose of the `render` method in a class component?

#### --distractors--

To define the component's initial state.

---

To return the component's UI representation.

---

To handle events in the component.

#### --answer--

To return the component's UI representation.

### --question--

#### --text--

What is a controlled component in React?

#### --distractors--

A component that maintains its own internal state.

---

A component that is driven by props rather than internal state.

---

A component that does not re-render when props change.

#### --answer--

A component that is driven by props rather than internal state.

### --question--

#### --text--

What is the purpose of the `useContext` hook?

#### --distractors--

To create a new context.

---

To consume values from a context.

---

To update the state of a component.

#### --answer--

To consume values from a context.

### --question--

#### --text--

What does `setState` do in a class component?

#### --distractors--

It directly modifies the component's state.

---

It schedules an update to the component's state object.

---

It initializes the component's state.

#### --answer--

It schedules an update to the component's state object.

### --question--

#### --text--

What are higher-order components in React?

#### --distractors--

Components that manage their own state.

---

Functions that take a component and return a new component.

---

Components that are rendered conditionally.

#### --answer--

Functions that take a component and return a new component.

### --question--

#### --text--

What is the purpose of `React.Fragment`?

#### --distractors--

To group multiple elements without adding extra nodes to the DOM.

---

To manage the state of multiple components.

---

To create a new component type.

#### --answer--

To group multiple elements without adding extra nodes to the DOM.
