---
id: 66f1a2009e65c9a40a26d51e
title: React Basics Quiz
challengeType: 8
dashedName: quiz-react-basics
---

# --description--

Answer all of the questions below correctly to pass the quiz.

# --quizzes--

## --quiz--

### --question--

#### --text--

How do you handle a click event in a React component?

#### --distractors--

`<button onClick='handleClick()'>Click me</button>`

---

`<button onClick={handleClick}>Click me</button>`

---

`<button click={handleClick}>Click me</button>`

#### --answer--

`<button onClick={handleClick}>Click me</button>`

### --question--

#### --text--

Which hook would you use to mimic componentDidMount?

#### --distractors--

useState

---

useEffect

---

useContext

#### --answer--

useEffect

### --question--

#### --text--

What does `<React.Fragment>` do?

#### --distractors--

Wraps components without adding extra DOM nodes.

---

Creates a new HTML element.

---

Deletes an existing element.

#### --answer--

Wraps components without adding extra DOM nodes.

### --question--

#### --text--

What is the purpose of React Context?

#### --distractors--

Managing component styles.

---

Passing data through the component tree without props.

---

Controlling component re-renders.

#### --answer--

Passing data through the component tree without props.

### --question--

#### --text--

How do you initialize a state variable in a functional component?

#### --distractors--

const count = 0;

---

const [count, setCount] = useState(0);

---

const state = 0;

#### --answer--

const [count, setCount] = useState(0);

### --question--

#### --text--

What is a con of using Vanilla JS instead of a framework?

#### --distractors--

Increased verbosity and manual DOM manipulation.

---

Automatic state management.

---

Built-in routing capabilities.

#### --answer--

Increased verbosity and manual DOM manipulation.

### --question--

#### --text--

How does a single-page application typically update the view?

#### --distractors--

By making a full server reload.

---

By updating only the necessary parts of the page.

---

By reloading the entire HTML document.

#### --answer--

By updating only the necessary parts of the page.

### --question--

#### --text--

What is the correct way to create a functional React component?

#### --distractors--

Using classes only.

---

Using the function keyword or arrow functions.

---

Using the React.Component only.

#### --answer--

Using the function keyword or arrow functions.

### --question--

#### --text--

Which of the following is not a valid JSX element?

#### --distractors--

`<img class='img'>`

---

`<div className='class'>`

---

`<p>Hello World</p>`

#### --answer--

`<img class='img'>`

### --question--

#### --text--

Which of the following is an example of a meta framework for React?

#### --distractors--

Vue.js

---

Next.js

---

Angular

#### --answer--

Next.js

### --question--

#### --text--

What tool would you use to bootstrap a new React project?

#### --distractors--

Webpack

---

npm

---

create-react-app

#### --answer--

create-react-app

### --question--

#### --text--

What command installs dependencies listed in package.json?

#### --distractors--

npm run

---

npm install

---

npm update

#### --answer--

npm install

### --question--

#### --text--

How would you pass a name prop to a child component?

#### --distractors--

`<ChildComponent props="name" />`

---

`<ChildComponent name="Vignesh" />`

---

`<ChildComponent {props} />`

#### --answer--

`<ChildComponent name="Vignesh" />`

### --question--

#### --text--

What is the correct way to conditionally render a component?

#### --distractors--

` <Home />`

---

`<Home>isLoggedIn</Home>`

---

`{isLoggedIn ? <Home /> : <Login />}`

#### --answer--

`{isLoggedIn ? <Home /> : <Login />}`

### --question--

#### --text--

Why should the key prop not be the array index?

#### --distractors--

It can cause unexpected bugs during state updates.

---

It's the recommended way to set keys.

---

It improves performance.

#### --answer--

It can cause unexpected bugs during state updates.

### --question--

#### --text--

What is the correct way to add an inline style in React?

#### --distractors--

style={{ color: 'blue' }}

---

style="color: blue"

---

style={{ color: blue }}

#### --answer--

style={{ color: 'blue' }}

### --question--

#### --text--

How do you initialize a state variable in a functional component?

#### --distractors--

const state = 0;

---

const count = 0;

---

const [count, setCount] = useState(0);

#### --answer--

const [count, setCount] = useState(0);

### --question--

#### --text--

What causes a React component to re-render?

#### --distractors--

Changing the state using setState.

---

Directly modifying the state variable.

---

Calling render() method.

#### --answer--

Changing the state using setState.

### --question--

#### --text--

How would you specify a required name prop using PropTypes?

#### --distractors--

name: PropTypes.bool

---

name: PropTypes.string.isRequired

---

name: PropTypes.string

#### --answer--

Passing data through the component tree without props.

### --question--

#### --text--

How would you create a route in React Router?

#### --distractors--

`<Link path='/home' component={Home} />`

---

`<Router path='/home' />`

---

`<Route path="/home" component={Home} />`

#### --answer--

`<Route path="/home" component={Home} />`
