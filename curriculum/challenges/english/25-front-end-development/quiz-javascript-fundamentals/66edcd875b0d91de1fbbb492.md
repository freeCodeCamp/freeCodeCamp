---
id: 66edcd875b0d91de1fbbb492
title: JavaScript Fundamentals Quiz
challengeType: 8
dashedName: quiz-javascript-fundamentals
---

# --description--

Answer all of the questions below correctly to pass the quiz.

# --quizzes--

## --quiz--

### --question--

#### --text--

What is the purpose of default parameters in a function?

#### --distractors--

To define a variable that cannot be changed.

---

To provide a fallback value when no argument is passed.

---

To enforce strict mode in JavaScript.

#### --answer--

To provide a fallback value when no argument is passed.

### --question--

#### --text--

Which syntax is used for a rest parameter in a function?

#### --distractors--

`function example(...args) { }`

---

`function example(args...) { }`

---

`function example(args[]) { }`

#### --answer--

`function example(...args) { }`

### --question--

#### --text--

What does destructuring allow you to do in JavaScript?

#### --distractors--

Assign variables from an array or object.

---

Create new objects from existing ones.

---

Declare constants in a function.

#### --answer--

Assign variables from an array or object.

### --question--

#### --text--

Which of the following is considered bad practice when naming variables?

#### --distractors--

Using camelCase.

---

Using descriptive names.

---

Using single-letter names for meaningful variables.

#### --answer--

Using single-letter names for meaningful variables.

### --question--

#### --text--

What is the primary purpose of a linter in JavaScript?

#### --distractors--

To convert JavaScript code to HTML.

---

To analyze code for potential errors and enforce coding standards.

---

To minify JavaScript files for production.

#### --answer--

To analyze code for potential errors and enforce coding standards.

### --question--

#### --text--

Why is memory management important in JavaScript?

#### --distractors--

To reduce the size of the JavaScript file.

---

To ensure variables are correctly scoped.

---

To prevent memory leaks and optimize performance.

#### --answer--

To prevent memory leaks and optimize performance.

### --question--

#### --text--

What is a significant drawback of using the `var` keyword?

#### --distractors--

Variables declared with `var` are block-scoped.

---

`var` declarations are hoisted to the top of the function.

---

`var` cannot be reassigned.

#### --answer--

`var` declarations are hoisted to the top of the function.

### --question--

#### --text--

What is hoisting in JavaScript?

#### --distractors--

Moving variable declarations to the end of their scope.

---

Creating a copy of an object.

---

The ability to declare a variable without initialization.

#### --answer--

Moving variable declarations to the end of their scope.

### --question--

#### --text--

Which statement correctly imports a module in JavaScript?

#### --distractors--

`import { moduleName } from 'module';`

---

`require('moduleName');`

---

`include 'moduleName';`

#### --answer--

`import { moduleName } from 'module';`

### --question--

#### --text--

What is the purpose of the `export` keyword in JavaScript?

#### --distractors--

To create a new variable.

---

To make functions or variables available outside of a module.

---

To initialize a module.

#### --answer--

To make functions or variables available outside of a module.

### --question--

#### --text--

Which of the following is a valid default parameter?

#### --distractors--

`function add(a, b = 0) { return a + b; }`

---

`function add(a, b: 0) { return a + b; }`

---

`function add(a, b) { b = b || 0; return a + b; }`

#### --answer--

`function add(a, b = 0) { return a + b; }`

### --question--

#### --text--

What is the output of the following code: `console.log(typeof null);`?

#### --distractors--

`"object"`

---

`"null"`

---

`"undefined"`

#### --answer--

`"object"`

### --question--

#### --text--

What is the result of the following code: `let x = [1, 2, 3]; let y = [...x];`?

#### --distractors--

`x` and `y` point to the same array.

---

`y` is a shallow copy of `x`.

---

`y` is an undefined variable.

#### --answer--

`y` is a shallow copy of `x`.

### --question--

#### --text--

Which statement correctly destructures an object?

#### --distractors--

`const { name, age } = person;`

---

`const [name, age] = person;`

---

`const name, age = person;`

#### --answer--

`const { name, age } = person;`

### --question--

#### --text--

What will the following code output: `console.log(0 == '0');`?

#### --distractors--

`true`

---

`false`

---

`undefined`

#### --answer--

`true`

### --question--

#### --text--

What is the difference between `==` and `===` in JavaScript?

#### --distractors--

`==` checks for value equality, while `===` checks for value and type equality.

---

There is no difference.

---

`===` is used for comparing objects only.

#### --answer--

`==` checks for value equality, while `===` checks for value and type equality.

### --question--

#### --text--

What is a closure in JavaScript?

#### --distractors--

A function that returns another function.

---

A function that is defined inside a loop.

---

A function that has no parameters.

#### --answer--

A function that returns another function.
