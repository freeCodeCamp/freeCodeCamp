---
id: 66f1b06a5a5d10cc100af620
title: Git Quiz
challengeType: 8
dashedName: quiz-git
---

# --description--

Answer all of the questions below correctly to pass the quiz.

# --quizzes--

## --quiz--

### --question--

#### --text--

What command is used to initialize a new Git repository?

#### --distractors--

`git start`

---

`git init`

---

`git new`

#### --answer--

`git init`

### --question--

#### --text--

Which command is used to create a new branch in Git?

#### --distractors--

`git create branch`

---

`git new-branch`

---

`git branch`

#### --answer--

`git branch`

### --question--

#### --text--

How do you stage changes for commit in Git?

#### --distractors--

`git stage .`

---

`git add .`

---

`git commit .`

#### --answer--

`git add .`

### --question--

#### --text--

What does the command `git commit -m "message"` do?

#### --distractors--

Commits changes without a message

---

Commits changes with a message

---

Stages changes with a message

#### --answer--

Commits changes with a message

### --question--

#### --text--

How do you view the commit history in Git?

#### --distractors--

`git history`

---

`git log`

---

`git commits`

#### --answer--

`git log`

### --question--

#### --text--

What is the purpose of the `.gitignore` file?

#### --distractors--

To ignore all Git commands

---

To specify files and directories to be ignored by Git

---

To ignore commits

#### --answer--

To specify files and directories to be ignored by Git

### --question--

#### --text--

What command do you use to clone a remote repository?

#### --distractors--

`git copy <repository-url>`

---

`git clone <repository-url>`

---

`git fetch <repository-url>`

#### --answer--

`git clone <repository-url>`

### --question--

#### --text--

How do you merge a branch into the current branch in Git?

#### --distractors--

`git merge <branch-name>`

---

`git join <branch-name>`

---

`git combine <branch-name>`

#### --answer--

`git merge <branch-name>`

### --question--

#### --text--

Which command is used to check the status of the working directory?

#### --distractors--

`git check`

---

`git status`

---

`git diff`

#### --answer--

`git status`

### --question--

#### --text--

What is a pull request in Git?

#### --distractors--

A request to pull the latest changes from the remote

---

A request to merge changes into the main branch

---

A request to delete a branch

#### --answer--

A request to merge changes into the main branch

### --question--

#### --text--

How do you resolve merge conflicts in Git?

#### --distractors--

`git resolve`

---

Manually edit the conflicting files and then commit

---

`git conflict`

#### --answer--

Manually edit the conflicting files and then commit

### --question--

#### --text--

What does `git stash` do?

#### --distractors--

Temporarily saves changes

---

Permanently deletes changes

---

Applies changes from the last commit

#### --answer--

Temporarily saves changes

### --question--

#### --text--

What is the purpose of a Git tag?

#### --distractors--

To mark specific points in the commit history

---

To create branches

---

To delete commits

#### --answer--

To mark specific points in the commit history

### --question--

#### --text--

Which command do you use to push changes to a remote repository?

#### --distractors--

`git send`

---

`git upload`

---

`git push`

#### --answer--

`git push`

### --question--

#### --text--

What does the command `git fetch` do?

#### --distractors--

Updates the local repository with remote changes

---

Downloads a specific file from the remote

---

Removes untracked files

#### --answer--

Updates the local repository with remote changes

### --question--

#### --text--

How can you view the differences between your local and remote branches?

#### --distractors--

`git compare`

---

`git diff`

---

`git status`

#### --answer--

`git diff`

### --question--

#### --text--

What is the difference between `git pull` and `git fetch`?

#### --distractors--

`git pull` fetches and merges, while `git fetch` only fetches

---

`git pull` fetches only, while `git fetch` fetches and merges

---

There is no difference

#### --answer--

`git pull` fetches and merges, while `git fetch` only fetches

### --question--

#### --text--

How do you remove a file from the staging area in Git?

#### --distractors--

`git unstage <file-name>`

---

`git remove <file-name>`

---

`git reset <file-name>`

#### --answer--

`git reset <file-name>`

### --question--

#### --text--

What does `git remote -v` display?

#### --distractors--

The list of local branches

---

The list of remote repositories

---

The list of commits

#### --answer--

The list of remote repositories

### --question--

#### --text--

How do you delete a branch in Git?

#### --distractors--

`git delete <branch-name>`

---

`git branch -d <branch-name>`

---

`git remove <branch-name>`

#### --answer--

`git branch -d <branch-name>`

### --question--

#### --text--

What command is used to reset the current branch to a previous commit?

#### --distractors--

`git undo <commit-id>`

---

`git reset --hard <commit-id>`

---

`git rollback <commit-id>`

#### --answer--

`git reset --hard <commit-id>`

### --question--

#### --text--

What is a bare repository in Git?

#### --distractors--

A repository without a working directory

---

A repository with no commits

---

A repository without branches

#### --answer--

A repository without a working directory
