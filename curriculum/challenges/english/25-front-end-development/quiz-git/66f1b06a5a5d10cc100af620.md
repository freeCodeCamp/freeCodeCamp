---
id: 66f1b06a5a5d10cc100af620
title: Git Quiz
challengeType: 8
dashedName: quiz-git
---

# --description--

Answer all the qs given below to pass the quiz!!
Good luck!

# --quizzes--

## --quiz--

### --question--

#### --text--

What is version control primarily used for?

#### --distractors--

To track changes in code over time.

---

To compile code into executable files.

---

To manage server configurations and edit memory specifications.

#### --answer--

To track changes in code over time.

### --question--

What is the primary benefit of using platforms like GitHub and GitLab?

#### --distractors--

They allow for unlimited storage of any file type, including videos and images.

---

They provide a platform for social networking via video call.

---

They enable collaboration and version control on code projects.

#### --answer--

They enable collaboration and version control on code projects.

### --question--

How do you install Git on a Unix-based system?

#### --distractors--

Use the command `apt-get install git`.

---

Download the Git installer from the official website.

---

Use the command `git install`.

#### --answer--

Use the command `apt-get install git`.

### --question--

What is an SSH key used for in Git?

#### --distractors--

It encrypts files for secure transfer.

---

It helps in managing user permissions.

---

It provides a secure way to connect to remote repositories.

#### --answer--

It provides a secure way to connect to remote repositories.

### --question--

What is a repository in Git?

#### --distractors--

A place where code is stored and tracked.

---

A graphical interface for editing code.

---

A remote server for deploying applications.

#### --answer--

A place where code is stored and tracked.

### --question--

Which command is used to push changes to a remote repository?

#### --distractors--

`git send`

---

`git push`

---

`git upload`

#### --answer--

`git push`

### --question--

How do you contribute to someone else's repository?

#### --distractors--

Fork the repository and make a pull request.

---

Clone the repository and push directly.

---

Send an email to the owner with your changes.

#### --answer--

Fork the repository and make a pull request.

### --question--

How do you create a new branch in Git?

#### --distractors--

`git branch new-branch`

---

`git checkout new-branch`

---

`git create new-branch`

#### --answer--

`git branch new-branch`

### --question--

What is a pull request?

#### --distractors--

A request to pull changes from one branch to another.

---

A command to merge branches automatically.

---

A way to report issues in the code.

#### --answer--

A request to pull changes from one branch to another.

### --question--

What does the code review process typically involve?

#### --distractors--

Automated tests running on all code.

---

Reviewing code for quality and suggesting changes.

---

Merging all branches without any review.

#### --answer--

Reviewing code for quality and suggesting changes.

### --question--

How can you deploy a project using GitHub Pages?

#### --distractors--

Push the project to the `main` branch of the repository.

---

Use the `git deploy` command.

---

Fork the repository and create a new branch.

#### --answer--

Push the project to the `main` branch of the repository.

### --question--

How do you remain in sync with a remote repository?

#### --distractors--

Use the `git sync` command.

---

Regularly pull changes from the remote repository.

---

Push your local changes every day.

#### --answer--

Regularly pull changes from the remote repository.

### --question--

What is a .gitignore file used for?

#### --distractors--

To list all files in a repository.

---

To exclude files from being tracked by Git.

---

To specify the branch to use for merging.

#### --answer--

To exclude files from being tracked by Git.

### --question--

What is a common branching strategy in Git?

#### --distractors--

Feature branching.

---

Single branching.

---

No branching at all.

#### --answer--

Feature branching.

### --question--

What is a merge conflict?

#### --distractors--

An error that occurs when two branches have conflicting changes.

---

A notification when a branch is deleted.

---

A command to reset the repository.

#### --answer--

An error that occurs when two branches have conflicting changes.

### --question--

How can you resolve a merge conflict?

#### --distractors--

Automatically merge changes without any input.

---

Manually edit the conflicting files and commit the changes.

---

Delete the conflicting branch.

#### --answer--

Manually edit the conflicting files and commit the changes.

### --question--

What does the `git stash` command do?

#### --distractors--

It permanently deletes changes.

---

It saves your changes temporarily.

---

It updates your local repository.

#### --answer--

It saves your changes temporarily.

### --question--

How do you squash commits in Git?

#### --distractors--

Use the `git merge` command.

---

Use the `git rebase -i` command.

---

Use the `git amend` command.

#### --answer--

Use the `git rebase -i` command.

### --question--

What is the difference between `git reset` and `git revert`?

#### --distractors--

`git reset` undoes commits, while `git revert` removes changes from history.

---

`git reset` changes the last commit, while `git revert` creates a new commit that undoes changes.

---

`git reset` keeps changes, while `git revert` permanently deletes them.

#### --answer--

`git reset` changes the last commit, while `git revert` creates a new commit that undoes changes.



Have you answered everything right??

