---
id: 66edd43cded6bff30944b676
title: Recursion Quiz
challengeType: 8
dashedName: quiz-recursion
---

# --description--

Answer all of the questions below correctly to pass the quiz.

# --quizzes--

## --quiz--

### --question--

#### --text--

What is Recursion?

#### --distractors--

When a function calls in other function, it is called recursion.

---

When a function calls inside the for loop, it is called recursion.

---

When a for loop created inside the function, it is called recursion. 

#### --answer--

When a function calls itself directly or indirectly, it is called recursion.

### --question--

#### --text--

What can we do with recursion?

#### --distractors--

We can reduce memory required for code.

---

We can get optimal time complexity.

---

We can handle large input.

#### --answer--

We can reduce size of code
### --question--

#### --text--

Which is not an example of recursion?

#### --distractors--

Tower Of Hanoi

---

Fibonacci Sequence

---

Binary Search

#### --answer--

Dijkstra's Algorithm

### --question--

#### --text--

Which are called base cases for recursion?

#### --distractors--

Which tells the function when to start.

---

Which tells the function when to print.

---

Which tells the function to run infinitly.

#### --answer--

which tells the function when to stop.

### --question--

#### --text--

Which is the base for recursive factorial function?

#### --distractors--

if n = 0:
    return 0 

---

if n = 1:
    return 2

---

if n = 1:
    return 0

#### --answer--

n = 0: 
    return 1

### --question--

#### --text--

What happen if we not add base cases in recursion?

#### --distractors--

Function does not run.

---

Function run for some time then stop.

---

Nothing happen!

#### --answer--

Function run for infinite time.

### --question--

#### --text--

Whatis the call stack?

#### --distractors--

is a counter that count time requred to run programm.

---

storage for all inbuilt functions.

---

is a data structure that stores information about the memory store by function.

#### --answer--

is a data structure that stores information about the active function calls in a program.

### --question--

#### --text--

Which value is returned firstly by the function in recursion?

#### --distractors--

None, all value retured at same time.

---

one before base case. 

---

first called function.

#### --answer--

when reach base case.

### --question--

#### --text--

When recursion stop?

#### --distractors--

after first function called.

---

one step before base case. 

---

Never stop.

#### --answer--

When reach base case.

### --question--

#### --text--

which is an example of recursion to find factorial?

#### --distractors--

def factorial(n):
    if n == 1:   
        return 1
    else:
        return (n + factorial(n - 1))  


---

def factorial(n):
    if n == 2:   
        return 1
    else:
        return n * factorial(n - 1)  


---

def factorial(n):
    result = 1
    for i in range(1, n + 1):
        result *= i
    return result


#### --answer--

def factorial(n):
    if n == 1:   
        return 1
    else:
        return n * factorial(n - 1)  


### --question--

#### --text--

In tail recursion what is the position of recursive call?

#### --distractors--

first

---

middle

---

second last

#### --answer--

last

### --question--

#### --text--

What is the benefit of tail recursion?

#### --distractors--

No benefit.

---

It required less time.

---

It can't be optimized by the computer.

#### --answer--

It can be optimized by the computer.

### --question--

#### --text--

How much recursive code can be converted into non-recursive code?

#### --distractors--

Some exeptions can be convertable.

---

Half can be convertable.

---

Nothing can be convertable.

#### --answer--

All can be convertable.

### --question--

#### --text--

In which manner the call stack operates?

#### --distractors--

First-In-Second-Out

---

First-In-First-Out

---

Last-In-Last-Out

#### --answer--

Last-In-First-Out

### --question--

#### --text--

Where each recursive call add stack(frame) in memory? 

#### --distractors--

All in same stack(frame)

---

Middle

---

Bottom

#### --answer--

Top

### --question--

#### --text--

Which is the correct code for fibonacci series?

#### --distractors--

def fibonacci(n):
    if n == 0:
        return 0
    elif n == 1:
        return 0
    else:
        return fibonacci(n) + fibonacci(n - 3)

---

def fibonacci(n):
    if n == 1:
        return 0
    elif n == 2:
        return 1
    else:
        return fibonacci(n - 1) + fibonacci(n - 2)

---

def fibonacci(n):
    if n == 0:
        return 0
    elif n == 1:
        return 1
    else:
        return fibonacci(n - 1) * fibonacci(n - 2)

#### --answer--

def fibonacci(n):
    if n == 0:
        return 0
    elif n == 1:
        return 1
    else:
        return fibonacci(n - 1) + fibonacci(n - 2)


### --question--

#### --text--

which is non-recursive form of below code?
def sum(n):
    if n == 1:
        return 1
    else:
        return n+sum(n-1)

#### --distractors--

def sum(n):
    return n

---

def sum(n):
    return (n+(n-1))

---

def sum(n):
    return n*(n-1)

#### --answer--

def sum(n):
    return (n*(n-1))/2

### --question--

#### --text--

def function(n):
    if n == 0:
        return 0
    print(n+function(n-1))
    return n
What is the output for n = 3?

#### --distractors--

5
3
1

---

3
2
1

---

1
2
3

#### --answer--

1
3
5

### --question--

#### --text--

def function(x, y):
    if y == 1:
        return 2*x + y - (x+y)*x
    function(2*x-y, y-1)

What is returned value for (x, y) = (2, 3) 

#### --distractors--

3

---

2

---

0

#### --answer--

1

### --question--

#### --text--

def function(a, b):
    if b == 0:
        return 0
    return (a + function(a*b, b-1))
What is the answer for (a, b) = (1, 3)

#### --distractors--

6

---

18

---

10

#### --answer--

12

