---
id: 66edd43cded6bff30944b676
title: Recursion Quiz
challengeType: 8
dashedName: quiz-recursion
---

# --description--

Answer all of the questions below correctly to pass the quiz.

# --quizzes--

## --quiz--

### --question--

#### --text--

What is recursion in programming?

#### --distractors--

A technique for repeating a block of code.

---

A method for organizing code efficiently.

---

A concept used only in object-oriented programming.

#### --answer--

A programming technique where a function calls itself.

### --question--

#### --text--

What will be the output of the following function call: `fibonacci(5)`?
```js
function fibonacci(n) {
    if (n <= 1) return n;
    return fibonacci(n - 1) + fibonacci(n - 2);
}
```

#### --distractors--

2

---

7

---

8

#### --answer--

5

### --question--

#### --text--

In the following code, what is the base case?
```js
function factorial(n) {
    if (n == 0) return 1;
    return n * factorial(n - 1);
}
```

#### --distractors--

`if (n > 0)`

---

`return n + 1;`

---

`n * 2`

#### --answer--

`if (n == 0)`

### --question--

#### --text--

In the following code, what does the recursive call represent?
```js
function printNumbers(n) {
    if (n > 0) {
        printNumbers(n - 1);
        console.log(n);
    }
}
```

#### --distractors--

It prints the numbers in ascending order.

---

It skips the number zero.

---

It multiplies the numbers by 2.

#### --answer--

It prints the numbers in reverse order.

### --question--

#### --text--

What is the time complexity of the recursive Fibonacci function?

#### --distractors--

O(n log n)

---

O(n^2)

---

O(n!)

#### --answer--

O(2^n)

### --question--

#### --text--

What is tail recursion?

#### --distractors--

A recursive function with no parameters.

---

A function that calls itself in the middle of execution.

---

A recursive function that returns multiple values.

#### --answer--

A recursion where the recursive call is the last statement.

### --question--

#### --text--

In the following function, what is the base case?
```js
function power(x, n) {
    if (n == 0) return 1;
    return x * power(x, n - 1);
}
```

#### --distractors--

`if (n > 0)`

---

`return x + power(x, n - 1);`

---

`x * 2`

#### --answer--

`if (n == 0)`

### --question--

#### --text--

What is the space complexity of a recursive function with n recursive calls?

#### --distractors--

O(n^2)

---

O(2^n)

---

O(log n)

#### --answer--

O(n)

### --question--

#### --text--

What is the output of the following code?
```js
function sum(n) {
    if (n == 0) return 0;
    return n + sum(n - 1);
}
```

#### --distractors--

The difference between the numbers.

---

The product of numbers from 1 to n.

---

An undefined value.

#### --answer--

The sum of numbers from 1 to n.

### --question--

#### --text--

Which of the following statements is true about recursive functions?

#### --distractors--

They never have a base case.

---

They modify local variables only.

---

They are faster than all iterative solutions.

#### --answer--

They always have a base case.

### --question--

#### --text--

What does the call stack do in a recursive function?

#### --distractors--

Executes the final return statement.

---

Stores the base case.

---

Tracks memory usage only.

#### --answer--

Both of the above.

### --question--

#### --text--

In the following code, what does `return 1;` represent?
```js
function countDown(n) {
    if (n <= 0) return 1;
    console.log(n);
    return countDown(n - 1);
}
```

#### --distractors--

The recursive case.

---

The stopping condition.

---

The function's main logic.

#### --answer--

The base case.

### --question--

#### --text--

What is head recursion?

#### --distractors--

A recursion where the base case is checked last.

---

A recursion with multiple base cases.

---

A recursion that processes all elements in a loop.

#### --answer--

A recursion where the recursive call is the first statement.

### --question--

#### --text--

What will be printed by the following code when `n = 3`?
```js
function printStar(n) {
    if (n > 0) {
        console.log("*");
        printStar(n - 1);
    }
}
```

#### --distractors--

2 stars

---

5 stars

---

No stars

#### --answer--

3 stars

### --question--

#### --text--

What is the purpose of the following line in a recursive function?
```js
return n * factorial(n - 1);
```

#### --distractors--

It skips the base case.

---

It ends the recursion.

---

It multiplies by 2.

#### --answer--

It is the recursive call.

### --question--

#### --text--

What is the purpose of a base case in a recursive function?

#### --distractors--

To skip unnecessary calculations.

---

To speed up recursion.

---

To reduce memory usage.

#### --answer--

To prevent infinite recursion.

### --question--

#### --text--

Which of the following is a drawback of using recursion?

#### --distractors--

Reduced code readability.

---

More lines of code.

---

Better performance.

#### --answer--

Potential stack overflow.

### --question--

#### --text--

What is the primary advantage of using recursion over iteration?

#### --distractors--

Lower execution time.

---

Less memory usage.

---

No need for function calls.

#### --answer--

Easier to read and maintain.

### --question--

#### --text--

What happens when a recursive function does not have a base case?

#### --distractors--

The program will finish after a long time.

---

The program will produce incorrect results.

---

The program will return null.

#### --answer--

The program will run indefinitely.
