| id                         | title                    | challengeType | dashedName               |
|---------------------------|--------------------------|----------------|--------------------------|
| 66f1b11026394bcdb7c42473  | Backend JavaScript Quiz   | 8              | quiz-backend-javascript   |


# --description--

Answer all of the questions below correctly to pass the quiz.

# --quizzes--

## --quiz--

### --question--

#### --text--

What does `require()` do in Node.js?

#### --distractors--

Imports ES6 modules

---

Executes asynchronous code

---

Adds a new route in Express

#### --answer--

Imports modules in Node.js

### --question--

#### --text--

How do you export a module in Node.js?

#### --distractors--

`module.send = myFunction;`

---

`export.myFunction = myFunction;`

---

`module.exportFunction = myFunction;`

#### --answer--

`module.exports = myFunction;`

### --question--

#### --text--

Which of the following is true about the `next()` function in Express middleware?

#### --distractors--

It sends a response to the client

---

It terminates the request

---

It is only called at the end of the request-response cycle

#### --answer--

It passes control to the next middleware function

### --question--

#### --text--

What does the `fs` module in Node.js handle?

#### --distractors--

Networking

---

Buffering data

---

Environment variables

#### --answer--

File system operations

### --question--

#### --text--

How do you connect a Node.js application to a MySQL database using the `mysql` package?

#### --distractors--

Using `fs.connect()`

---

Using `pg.connect()`

---

Using `http.connect()`

#### --answer--

Using `mysql.createConnection()`

### --question--

#### --text--

Which method is used to define a route for POST requests in Express?

#### --distractors--

`app.route()`

---

`app.use()`

---

`app.get()`

#### --answer--

`app.post()`

### --question--

#### --text--

What is the default behavior of `npm install`?

#### --distractors--

Installs packages globally

---

Runs tests for installed packages

---

Uninstalls unnecessary dependencies

#### --answer--

Installs the project dependencies locally

### --question--

#### --text--

What is the purpose of the `package.json` file in a Node.js project?

#### --distractors--

It manages routing in Express

---

It connects Node.js to databases

---

It configures the network stack in Node.js

#### --answer--

It holds metadata and dependencies of the project

### --question--

#### --text--

How do you handle an error in Express middleware?

#### --distractors--

Using `app.onError()`

---

Using `app.catch()`

---

Using `app.finally()`

#### --answer--

Pass the error to the next middleware by calling `next(err)`

### --question--

#### --text--

What is the default scope of an npm package installation?

#### --distractors--

System-wide

---

User-wide

---

Globally on all servers

#### --answer--

Local to the project folder

### --question--

#### --text--

Which method in Express is used to send a JSON response?

#### --distractors--

`res.write()`

---

`res.jsonp()`

---

`res.end()`

#### --answer--

`res.json()`

### --question--

#### --text--

Which runtime is used by Node.js to execute JavaScript code?

#### --distractors--

Deno

---

ChakraCore

---

Vercel

#### --answer--

V8

### --question--

#### --text--

How do you install a specific version of a package using npm?

#### --distractors--

`npm get <package>@<version>`

---

`npm fetch <package>@<version>`

---

`npm download <package>@<version>`

#### --answer--

`npm install <package>@<version>`

### --question--

#### --text--

How do you define a catch-all route in Express for handling 404 errors?

#### --distractors--

`app.all('*', handler)`

---

`app.post('*', handler)`

---

`app.route('*', handler)`

#### --answer--

`app.use('*', handler)`

### --question--

#### --text--

Which of the following is NOT a valid Express middleware function?

#### --distractors--

`res.json()`

---

`app.use()`

---

`app.post()`

#### --answer--

`res.json()`

### --question--

#### --text--

How do you stop a running Node.js server programmatically?

#### --distractors--

By calling `app.exit()`

---

By sending a `stop` signal

---

By using `res.sendExit()`

#### --answer--

By calling `process.exit()`

### --question--

#### --text--

What is the purpose of `dotenv` in a Node.js project?

#### --distractors--

To load default dependencies

---

To configure routes automatically

---

To manage npm package versions

#### --answer--

To load environment variables from a `.env` file

### --question--

#### --text--

How do you serve static files in Express?

#### --distractors--

By using `app.files()`

---

By using `app.staticFiles()`

---

By using `app.get()`

#### --answer--

By using `app.use(express.static('folderName'))`

### --question--

#### --text--

How can you run a Node.js script using a specific version of Node if multiple versions are installed?

#### --distractors--

Use `npm use <version> <script>`

---

Use `node select <version> <script>`

---

Use `node fetch <version> <script>`

#### --answer--

Use `nvm use <version>` and then run the script

### --question--

#### --text--

What does `express.Router()` do in Express.js?

#### --distractors--

It automatically generates routes based on file structure

---

It is used for error handling only

---

It replaces the need for middleware

#### --answer--

It creates a new router object to handle routes modularly
