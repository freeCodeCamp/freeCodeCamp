---
id: 6854085b475edfa6800e992c
title: Step 19
challengeType: 20
dashedName: step-19
---

# --description--

Let's run the `User` class methods you recently created.

Create 2 `User` object named `alice` and `bob`. Then, have `alice` send an email to `bob` with the subject "Meeting Update" and the body "The meeting is now at 4 PM.".
After that, have `bob` check his inbox using `list_emails` and read the first email.

<!-- new step to be added -->

solution:

# Create users
alice = User("Alice")
bob = User("Bob")

# Alice sends an email to Bob
alice.send_email(bob, "Meeting Update", "The meeting is now at 4 PM.")

# Bob checks his inbox
bob.inbox.list_emails()

# Bob reads the first email
bob.inbox.read_email(0)

<!-- end -->

Users need a way to check their inbox. This method will display the user's name and then show all their emails.

Add a method called `check_inbox` to your `User` class. This method should print a header with the user's name like this:

```md
'\n[user]'s Inbox:
```

After that, call the `Inbox` class' `list_emails` method on the user's inbox to display the emails.

# --hints--

You should define a method named `check_inbox` in the `User` class that takes `self` as a parameter.

```js

```

You should print a header that includes the user's name in the format `'\n[user]'s Inbox:`.

```js

```

You should call `self.inbox.list_emails()` to display the emails.

```js

```

# --seed--

## --seed-contents--

```py
import datetime

class Email:
    def __init__(self, sender, receiver, subject, body):
        self.sender = sender
        self.receiver = receiver
        self.subject = subject
        self.body = body
        self.timestamp = datetime.datetime.now()
        self.read = False

    def mark_as_read(self):
        self.read = True

    def display_full_email(self):
        self.mark_as_read()
        print('\n--- Email ---')
        print(f'From: {self.sender.name}')
        print(f'To: {self.receiver.name}')
        print(f'Subject: {self.subject}')
        print(f"Received: {self.timestamp.strftime('%Y-%m-%d %H:%M')}")
        print(f'Body: {self.body}')
        print('------------\n')

    def __str__(self):
        status = 'Read' if self.read else 'Unread'
        return f"[{status}] From: {self.sender.name} | Subject: {self.subject} | Time: {self.timestamp.strftime('%Y-%m-%d %H:%M')}"

class Inbox:
    def __init__(self):
        self.emails = []

    def receive_email(self, email):
        self.emails.append(email)

    def list_emails(self):
        if not self.emails:
            print('Your inbox is empty.\n')
            return
        print('\nYour Emails:')
        for i, email in enumerate(self.emails, start=1):
            print(f'{i}. {str(email)}')


    def read_email(self, index):
        if not self.emails:
            print('Inbox is empty.\n')
            return
        try:
            self.emails[index].display_full_email()
        except IndexError:
            print('Invalid email number.\n')

    def delete_email(self, index):
        if not self.emails:
            print('Inbox is empty.\n')
            return
        try:
            del self.emails[index]
            print('Email deleted.\n')
        except IndexError:
            print('Invalid email number.\n')
    def send_email(self, receiver, subject, body):
        email = Email(sender=self, receiver=receiver, subject=subject, body=body)
        receiver.inbox.receive_email(email)
        print(f'Email sent from {self.name} to {receiver.name}!\n')

--fcc-editable-region--
        

--fcc-editable-region--
```
