---
id: 69bc83a7fbcca221a47185da
title: Step 7
challengeType: 0
dashedName: step-7
---

# --description--

**Let's draw the y-axis**  
In contrast of x-axis, y-axis will be mapped to (0, max_value_on_y_axis) to (max_height, 0). 

```js
const y = d3
  .scaleLinear()
  .domain([0, 10])
  .range([height, 0]);
```

Now using this scale we can create yAxix by axis generator **axisLeft()** 

```js
const yAxis = d3.axisLeft(y);
```

Now, we will draw the axis inside a group `g`, using **call()** builder method and append it to the `svg`.  
Additionally, we will add a vertical text label for this axis, **Note that** after rotating -90 degrees texts orientation is also got changed. 

```js
svg
  .append("g")
  .call(yAxis)
  .append("text")
  .attr("transform", "rotate(-90)")    // rotate the label
  .attr("x", -height / 2)              // center vertically (negative because of rotation)
  .attr("y", -margin.left + 15)        // move left of the axis line
  .style("text-anchor", "middle")      // center the text
  .style("fill", "black")              // ensure visible color
  .style("font-size", windowWidth > 700 ? "12px" : "16px")
  .text(windowWidth > 700 ? "Lifespan (years)" : "Weight (kg)");
```

Now add y-axis with previously added x-axis and examine the preview. Playing with co-ordinates will clarify all your questions.


# --hints--

Test 1

```js

```

# --seed--

## --seed-contents--

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Zoo Animal Scatterplot</title>
    <link rel="stylesheet" href="styles.css" />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/d3/7.9.0/d3.min.js"></script>
  </head>
  <body>
    <script src="script.js"></script>
  </body>
</html>
```

```css
.graph {
  display: block;
  margin: auto;
  border: 1px solid #000;
  background-color: white;
}

body {
  font: 10px sans-serif;
}

.axis path,
.axis line {
  fill: none;
  stroke: #000;
  shape-rendering: crispEdges;
}

.dot {
  stroke: #000;
  opacity: 0.8;
}

div.tooltip {
  position: absolute;
  padding: 10px;
  font: 12px sans-serif;
  background: lightsteelblue;
  border-radius: 8px;
  pointer-events: none;
}
```

```js
const margin = { top: 60, right: 60, bottom: 50, left: 60 };
const maxHeight = 500;
const maxWidth = 800;

const svg = d3
  .select("body")
  .append("svg")
  .attr("class", "graph")
  .append("g")
  .attr("transform", `translate(${margin.left},${margin.top})`);

function drawScatterplot() {
  const windowWidth = window.innerWidth;
  let width = Math.min(maxWidth, windowWidth * 0.9) - margin.left - margin.right;
  let height = Math.min(maxHeight, window.innerHeight * 0.7) - margin.top - margin.bottom;

  d3.select("svg")
    .attr("width", width + margin.left + margin.right)
    .attr("height", height + margin.top + margin.bottom);
    
  const x = d3
    .scaleLinear()
    .domain([0, 100])
    .range([0, width]);
  const xAxis = d3.axisBottom(x);

  svg
    .append("g")
    .attr("transform", `translate(0,${height})`)
    .call(xAxis)
    .append("text")           
    .attr("x", width / 2)    
    .attr("y", 40)           
    .style("text-anchor", "middle")
    .style("fill", "black")   
    .style("font-size", windowWidth > 700 ? "12px" : "16px")
    .text(windowWidth > 700 ? "Weight (kg)" : "Lifespan (years)");

  --fcc-editable-region--

  --fcc-editable-region--
}

drawScatterplot();
window.addEventListener("resize", drawScatterplot);
```
