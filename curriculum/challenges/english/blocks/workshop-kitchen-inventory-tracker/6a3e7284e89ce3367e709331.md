---
id: 6a3e7284e89ce3367e709331
title: Step 13
challengeType: 20
dashedName: step-13
---

# --description--

Now you are going to solve the mystery from earlier! When your `use_eggs` function returned the remaining number of eggs, you didn't save that new value anywhere. It just disappeared into the void! 

To actually update your inventory, you need to capture that returned value by assigning it to a variable. 

Inside your `make_fried_egg` function, replace the `pass` keyword. First, create a variable named `has_enough_eggs` and assign it the result of comparing if `eggs` is greater than or equal to 1.



# --hints--

You should assign `has_enough_eggs` to the result of checking if `eggs` is greater than or equal to `1`.

```js
({
  test: () => assert(
    runPython(`_Node(_code).find_function("make_fried_egg").find_variables("has_enough_eggs")[0].is_equivalent("has_enough_eggs = eggs >= 1")`)
  )
})
```

# --seed--

## --seed-contents--

```py
eggs = 1
flour = 2
sugar = 3

def take_inventory():
    total_items = eggs + flour + sugar
    print(f'The kitchen has {total_items} total items:')
    print(f'- {eggs} eggs')
    print(f'- {flour} flour')
    print(f'- {sugar} sugar')

take_inventory()

def use_eggs(available_eggs, eggs_to_use):
    if eggs_to_use > available_eggs:
        print('The kitchen does not have enough eggs.')
        return available_eggs

    print(f'{eggs_to_use} egg(s) used out of {available_eggs} available.')
    return available_eggs - eggs_to_use

use_eggs(eggs, 1)
print(eggs)
        
def make_fried_egg(eggs):
--fcc-editable-region--
    pass
--fcc-editable-region--
```
