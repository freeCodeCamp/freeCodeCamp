---
id: 69a56b5069ca99f7317e6e19
title: Build a Cargo Manifest Validator
challengeType: 26
dashedName: lab-cargo-manifest-validator
---

# --description--

In this lab, you will leverage JavaScript to normalize and validate cargo manifests. A cargo manifest is a document that typically lists goods being transported (for example, by ship or train) and includes details about those goods.

Each cargo manifest will be represented as an object with the following structure:

```js
{
  containerId: 0,
  destination: "Monterey, California, USA",
  weight: 340.50,
  unit: "lb", 
  hazmat: false
}
```

Note that each cargo manifest object should contain: 
    - `containerId`: a unique integer value identifying the cargo container
    - `destination`: a string denoting the cargo's target destination
    - `weight`: a numerical float value representing the cargo's weight
    - `unit`: a string describing the units for the cargo's weight property (either "kg" for kilograms or "lb" for pounds).
    - `hazmat`: a boolean value indicating whether hazardous material handling is needed

**Objective:** Fulfill the user stories below and get all the tests to pass to complete the lab.

**User Stories:**

1. You should implement the function `normalizeUnits(manifest)`, which converts a manifest object's weight to kilograms without mutating the source object (return a new object). Use the approximate conversion, 1 lb = 0.45 kg.
2. You should implement the function `validateManifest(manifest)`, which accepts a manifest object and:
    - If the input manifest is valid (no missing or invalid fields), returns an empty object `{}`.
    - If any of the input manifest's fields are invalid or missing, returns an object describing those fields; specifically, `missingField: "Missing"` and `invalidField: "Invalid"`. Here is an example return where the input object is missing the `destination` field and has an invalid `weight` field:

    ```js
    {
        destination: "Missing",
        weight: "Invalid"
    }
    ```
    
    - A field is invalid if: 
        - `destination` is not a non-empty string
        - `weight` is not a positive number
        - `unit` is neither "kg" nor "lb"
        - `hazmat` is not a boolean
3. You should implement the function `processManifest(manifest)`, which accepts a manifest object, and:
    - If the manifest is valid, logs `Validation success: ${containerId}` and then logs the manifest's weight in kilograms in the following format `Total weight: ${weight} kg`.
    - If the manifest is not valid, logs: `Validation error: ${containerId}` and then logs the object which is returned by calling `validateManifest()` on the original manifest.
    - Note: for this function, you can and should reuse the `normalizeUnits()` and `validateManifest()` functions.

# --hints--

You should have a function named `normalizeUnits` which accepts a manifest object as a parameter and returns a new copy of the manifest object with the weight converted to kilograms. Use the approximate conversion, 1 lb = 0.45 kg.

```js

```

You should 

```js

```

You should 

```js

```

You should have a function named `validateManifest` which accepts a manifest object as a parameter and, if valid, returns either an empty object `{}` or, if not valid, returns an object describing missing and/or invalid fields.

```js

```

You should 

```js

```

You should 

```js

```

You should have a function named `processManifest` which accepts a manifest object as a parameter and, if valid, logs a success message, the object's `containerId` and the object's `weight` in kilograms, or, if not valid, logs an error message, the object's `containerId` and the object itself.

```js

```

You should 

```js

```

You should 

```js

```

# --seed--

## --seed-contents--

```js

```

## --solutions--

```js
function normalizeUnits(manifest) {
    const normalized = {...manifest}
    if (manifest.unit === "lb") {
        normalized.weight = normalized.weight * 0.45;
        normalized.unit = "kg";
    }
    return normalized;
}

function validateManifest(manifest) {
    let result = {}

    if (manifest.destination === undefined) {
        result.destination = "Missing";
    }
    else if (typeof manifest.destination !== "string" || manifest.destination.trim() === "") {
        result.destination = "Invalid";
    }
    if (manifest.weight === undefined) {
        result.weight = "Missing";
    }
    else if (typeof manifest.weight !== "number" || manifest.weight <= 0) {
        result.weight = "Invalid";
    }
    if (manifest.unit === undefined) {
        result.unit = "Missing";
    }
    else if (manifest.unit !== "kg" && manifest.unit !== "lb") {
        result.unit = "Invalid";
    }
    if (manifest.hazmat === undefined) {
        result.hazmat = "Missing";
    }
    else if (typeof manifest.hazmat !== "boolean") {
        result.hazmat = "Invalid";
    }

    return result;
}

function processManifest(manifest) {
    const validation = validateManifest(manifest);

    if (Object.keys(validation).length > 0) {
        console.log(`Validation error: ${manifest.containerId}`);
        console.log(validation);
    } else {
        const normalized = normalizeUnits(manifest);
        console.log(`Validation success: ${manifest.containerId}`);
        console.log(`Total weight: ${normalized.weight} kg`);
    }

}
```
