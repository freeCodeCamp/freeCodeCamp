---
id: 6a70a88fe27c85f91d5137e8
title: Step 14
challengeType: 1
dashedName: step-14
---

# --description--

Each entry in `departmentEntries` is itself a small array in the shape `[key, value]`. Instead of accessing these by index (`entry[0]`, `entry[1]`), you can use array destructuring to unpack them into named variables in one line.

Destructure `departmentEntries[i]` into `departmentName` and `departmentData` and log these two variable into console.

# --hints--

Test 1

```js

```

# --seed--

## --seed-contents--

```js
const rawData = {
  "Computer Science": {
    Dept_code: "CSE",
    programs: [
      {
        id: "CSE101",
        name: "Database Management System",
        room: "402",
        instructors: [
          { id: "INS001", name: "Alejandro", email: "alejandro@edu.com" },
          { id: "INS002", name: "Kenji", officeHours: null },
        ],
      },
      {
        id: "CSE102",
        name: "Data Structures",
        room: "205",
        instructors: [
          { id: "INS003", name: "Fatima", email: "fatima@edu.com" },
        ],
      },
    ],
  },
  "Business Administration": {
    Dept_code: "BBA",
    programs: [
      {
        id: "BBA101",
        name: "Financial Accounting",
        room: "201",
        instructors: [{ id: "INS004", name: "Sofia", email: "sofia@edu.com" }],
      },
    ],
  },
};

function normalizeDirectory(rawData) {
  const departmentsById = {};
  const programsById = {};
  const instructorsById = {};
  const instructorsByEmail = {};

  const departmentEntries = Object.entries(rawData);

  for (let i = 0; i < departmentEntries.length; i++) {

    --fcc-editable-region--

    --fcc-editable-region--
  }
}

normalizeDirectory(rawData);
```
