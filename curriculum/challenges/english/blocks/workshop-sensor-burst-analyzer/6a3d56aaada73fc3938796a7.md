---
id: 6a3d56aaada73fc3938796a7
title: Step 6
challengeType: 1
dashedName: step-6
---

# --description--

Write a `for` loop that iterates over `sensorBurst`. Inside the loop, call `detectSilence` on the current burst. If it returns `true`, push the burst into `silentBursts`. Otherwise, push it into `activeBursts`.

# --hints--

`activeBursts` should contain 14 bursts after the loop.

```js

```

`silentBursts` should contain 7 bursts after the loop.

```js

```

Every burst in `activeBursts` should have no zero readings.

```js

```

Every burst in `silentBursts` should contain at least one zero.

```js

```

# --seed--

## --seed-contents--

```js
const sensorBurst = [
  [52, 77],
  [65, 99],
  [8, 72, 68],
  [25, 6],
  [93, 42, 23],
  [48, 33, 9],
  [0, 0],
  [84, 3, 23],
  [0, 0, 0],
  [0, 0],
  [0, 0, 0],
  [51, 16],
  [7, 71, 47],
  [16],
  [0, 0, 0],
  [99, 53],
  [0, 0],
  [74, 9],
  [22, 11],
  [55, 14, 39],
  [0, 0, 0]
];

function detectSilence(burst) {
  const sample = [...burst];

  if (sample.includes(0)) {
    return true;
  }
  return false;
}

const activeBursts = [];
const silentBursts = [];

--fcc-editable-region--

--fcc-editable-region--

```
