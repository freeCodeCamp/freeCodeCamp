---
id: 6a3d8fd28235ded30e3c7610
title: Step 9
challengeType: 1
dashedName: step-9
---

# --description--

Now you will build a function to extract a consistent window of readings from a burst, regardless of its total length. Declare a function called `getWindow` that takes `burst`, `start`, and `windowSize` as parameters. Leave the body empty for now.

# --hints--

You should have a `getWindow` function.

```js

```

`getWindow` should accept three parameters.

```js

```

# --seed--

## --seed-contents--

```js
const sensorBurst = [
  [52, 77],
  [65, 99],
  [8, 72, 68],
  [25, 6],
  [93, 42, 23],
  [48, 33, 9],
  [0, 0],
  [84, 3, 23],
  [0, 0, 0],
  [0, 0],
  [0, 0, 0],
  [51, 16],
  [7, 71, 47],
  [16],
  [0, 0, 0],
  [99, 53],
  [0, 0],
  [74, 9],
  [22, 11],
  [55, 14, 39],
  [0, 0, 0]
];

function detectSilence(burst) {
  const sample = [...burst];

  if (sample.includes(0)) {
    return true;
  }
  return false;
}

--fcc-editable-region--

--fcc-editable-region--

const activeBursts = [];
const silentBursts = [];

for (let i = 0; i < sensorBurst.length; i++) {
  if (detectSilence(sensorBurst[i])) {
    silentBursts.push(sensorBurst[i]);
  } else {
    activeBursts.push(sensorBurst[i]);
  }
}

```
