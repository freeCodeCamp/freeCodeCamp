---
id: 6a3d8cc6bd053426101a0174
title: Step 8
challengeType: 1
dashedName: step-8
---

# --description--

Now, clear up your logs and move on to building your next tool. Remove the two `console.log()` statements.

# --hints--

You should not have a `console.log("Active bursts:", activeBursts);` line in your code.

```js

```

You should not have a `console.log("Silent bursts:", silentBursts);` line in your code.

```js

```

# --seed--

## --seed-contents--

```js
const sensorBurst = [
  [52, 77],
  [65, 99],
  [8, 72, 68],
  [25, 6],
  [93, 42, 23],
  [48, 33, 9],
  [0, 0],
  [84, 3, 23],
  [0, 0, 0],
  [0, 0],
  [0, 0, 0],
  [51, 16],
  [7, 71, 47],
  [16],
  [0, 0, 0],
  [99, 53],
  [0, 0],
  [74, 9],
  [22, 11],
  [55, 14, 39],
  [0, 0, 0]
];

function detectSilence(burst) {
  const sample = [...burst];

  if (sample.includes(0)) {
    return true;
  }
  return false;
}

const activeBursts = [];
const silentBursts = [];

for (let i = 0; i < sensorBurst.length; i++) {
  if (detectSilence(sensorBurst[i])) {
    silentBursts.push(sensorBurst[i]);
  } else {
    activeBursts.push(sensorBurst[i]);
  }
}

--fcc-editable-region--
console.log("Active bursts:", activeBursts);
console.log("Silent bursts:", silentBursts);
--fcc-editable-region--

```
