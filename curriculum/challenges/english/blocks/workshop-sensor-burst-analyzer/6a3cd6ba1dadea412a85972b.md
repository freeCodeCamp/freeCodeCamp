---
id: 6a3cd6ba1dadea412a85972b
title: Step 1
challengeType: 1
dashedName: step-1
---

# --description--

You are going to build a Sensor Burst Analyzer. Sensors send readings in groups called bursts, which are arrays of numbers. Some sensors go silent (all zeros), some have faulty spikes, and bursts have inconsistent lengths. 

Declare a `const` variable called `sensorBurst` and assign it the nested array of sensor readings provided below.

```js
[
  [52, 77],
  [65, 99],
  [8, 72, 68],
  [25, 6],
  [93, 42, 23],
  [48, 33, 9],
  [0, 0],
  [84, 3, 23],
  [0, 0, 0],
  [0, 0],
  [0, 0, 0],
  [51, 16],
  [7, 71, 47],
  [16],
  [0, 0, 0],
  [99, 53],
  [0, 0],
  [74, 9],
  [22, 11],
  [55, 14, 39],
  [0, 0, 0]
]
```

# --hints--

You should have a `sensorBurst` variable declared with `const`.

```js

```

`sensorBurst` should be an array.

```js

```

`sensorBurst` should have 21 elements.

```js

```

Each element of `sensorBurst` should be an array.

```js

```

# --seed--

## --seed-contents--

```js
--fcc-editable-region--

--fcc-editable-region--
```
