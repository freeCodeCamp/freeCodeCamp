---
id: 6a3d8b8059bdd25a283b1ee1
title: Step 7
challengeType: 1
dashedName: step-7
---

# --description--

Log `activeBursts` and `silentBursts` to the console so you can see which bursts came from live sensors and which came from offline ones.

# --hints--

`activeBursts` should have 14 elements.

```js

```

`silentBursts` should have 7 elements.

```js

```

You should log `activeBursts` and `silentBursts` to the console.

```js

```

# --seed--

## --seed-contents--

```js
const sensorBurst = [
  [52, 77],
  [65, 99],
  [8, 72, 68],
  [25, 6],
  [93, 42, 23],
  [48, 33, 9],
  [0, 0],
  [84, 3, 23],
  [0, 0, 0],
  [0, 0],
  [0, 0, 0],
  [51, 16],
  [7, 71, 47],
  [16],
  [0, 0, 0],
  [99, 53],
  [0, 0],
  [74, 9],
  [22, 11],
  [55, 14, 39],
  [0, 0, 0]
];

function detectSilence(burst) {
  const sample = [...burst];

  if (sample.includes(0)) {
    return true;
  }
  return false;
}

const activeBursts = [];
const silentBursts = [];

for (let i = 0; i < sensorBurst.length; i++) {
  if (detectSilence(sensorBurst[i])) {
    silentBursts.push(sensorBurst[i]);
  } else {
    activeBursts.push(sensorBurst[i]);
  }
}

--fcc-editable-region--

--fcc-editable-region--

```
