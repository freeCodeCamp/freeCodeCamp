---
id: 6958e0ecc121b0ae3dd9c5e0
title: Step 7
challengeType: 20
dashedName: step-7
---

# --description--

You should also display the updated value of `discount` in the terminal. You can print a message followed by a variable like this:

```py
print('Total:', total)
```

Use the `print()` function to display the message that shows `Discount:` followed by the value of `discount`.

Check the output in the terminal.

# --hints--

You should use the `print()` function.

```js

```

You should use `Discount:` as first argument.

```js

```

You should use the `discount` variable as second argument.

```js

```

# --seed--

## --seed-contents--

```py
base_price = 15
age = 21

seat_type = 'Gold'
show_time = 'Evening'

is_member = True
is_weekend = False

if age > 18:
    print('User is eligible to book a ticket')

if age >= 21:
    print('User is eligible for Evening shows')
else:
    print('User is not eligible for Evening shows')

discount = 0
if age >= 21 and is_member:
    discount = 3
    print('User qualify for membership discount')
else:
    print('User does not qualify for membership discount')
--fcc-editable-region--

--fcc-editable-region--
```
