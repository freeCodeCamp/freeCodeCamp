---
id: 6958dcf685369045648d60bf
title: Step 3
challengeType: 20
dashedName: step-3
---

# --description--

Some of the informations can only be True or False. As you have learned in previous lessons, Booleans are used to store and truthy or falsy values.

Create a variable named `is_member` to indicate whether the user is a member and set its value to `True`.

Below the `is_member` variable create another variable named `is_weekend` to indicate whether the movie show is on a weekend and sets its value to `False`.

# --hints--

You should declare a variable named `is_member`.

```js

```

You should assign the boolean value `True` to `is_member`.

```js

```

You should declare a variable named `is_weekend`.

```js

```

You should assign the boolean value `False` to `is_weekend`.

```js

```

# --seed--

## --seed-contents--

```py
base_price = 15
age = 21

seat_type = 'Gold'
show_time = 'Evening'

--fcc-editable-region--

--fcc-editable-region--
```
