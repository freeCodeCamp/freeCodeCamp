---
id: 694648acde178bb8202d9513
title: Step 4
challengeType: 0
dashedName: step-4
---

# --description--

The student name should follow the same format as the other details.

Remove the earlier outputs of the `name` variable. Then, print `name` and `type(name)` together on one line like the previous step.

# --hints--

Remove `print()` statements from Step 1 and 2.

```js

```

Print `name` and `type(name)` using a comma separator.

```js

```

# --seed--

## --seed-contents--

```py
name = 'Alice'

--fcc-editable-region--
print(name)
print(type(name))
--fcc-editable-region--

is_student = True
print(is_student, type(is_student))
```
