---
id: 694648acde178bb8202d9512
title: Step 3
challengeType: 0
dashedName: step-3
---

# --description--

The report card should also show whether the student is currently enrolled.

Declare a variable named `is_student` and assign it the value `True`. This value represents a yes-or-no condition.

Then, print both `is_student` and the result of `type(is_student)` on the same line with a comma separator `,`. Check the output in the console.

```py
print('Hello', type('Hello')) # Output: Hello <class 'str'>
```

# --hints--

The variable `is_student` should store the value `True`.

```js

```

Print `is_student` and `type(is_student)` using a comma separator.

```js

```

# --seed--

## --seed-contents--

```py
name = 'Alice'
print(name)
print(type(name))

--fcc-editable-region--

--fcc-editable-region--
```
