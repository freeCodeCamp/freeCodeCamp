---
id: 694648acde178bb8202d9516
title: Step 7
challengeType: 0
dashedName: step-7
---

# --description--

The result is `False`, which shows that `score` is not an `int`.

Another common kind of number in Python is `float`. Replace `int` with `float` in the `isinstance()` check to confirm this.

Finally, print `score` and its data type to complete the report card.

# --hints--

Use `isinstance(score, float)` and print the result.

```js

```

Print `score` and `type(score)` using a comma separator.

```js

```

# --seed--

## --seed-contents--

```py
name = 'Alice'
print(name, type(name))

is_student = True
print(is_student, type(is_student))

age = 20
print(age, type(age))

score = 80.5

--fcc-editable-region--
print(isinstance(score, int))

--fcc-editable-region--

```
