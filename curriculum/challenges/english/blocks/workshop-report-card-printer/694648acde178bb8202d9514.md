---
id: 694648acde178bb8202d9514
title: Step 7
challengeType: 20
dashedName: step-7
---

# --description--

Add the student’s age to the report card.

Declare a variable named `age` and assign it the value `20`.

Then, print the value and data type of `age` together once more. Check the output in the console.

# --hints--

You should have an `age` variable.

```js

```

The variable `age` should store the value `20`.

```js

```

Print `age` and `type(age)` using a comma separator.

```js

```

# --seed--

## --seed-contents--

```py
name = 'Alice'
print(name, type(name))

is_student = True
print(is_student, type(is_student))

--fcc-editable-region--

--fcc-editable-region--

```
