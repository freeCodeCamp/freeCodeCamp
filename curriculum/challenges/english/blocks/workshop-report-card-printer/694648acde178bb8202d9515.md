---
id: 694648acde178bb8202d9515
title: Step 6
challengeType: 0
dashedName: step-6
---

# --description--

Now, add the student’s score.

Declare a variable named `score` and assign it the value `80.5`.

Although both `age` and `score` are numbers, they may not be the same kind. Python provides a function called `isinstance()` to check this.

```html
<div>Example Code</div>
<code>x = 10</code>
<code>print(isinstance(x, int)) # Output: True</code>
```

Use `isinstance()` to check whether `score` is an `int`, and print the result as the example.

# --hints--

The variable `score` should store the value `80.5`.

```js

```

Use `isinstance(score, int)` and print the result.

```js

```

# --seed--

## --seed-contents--

```py
name = 'Alice'
print(name, type(name))

is_student = True
print(is_student, type(is_student))

age = 20
print(age, type(age))

--fcc-editable-region--

--fcc-editable-region--

```
