---
id: 6921dae10700e300f26994e3
title: Step 7
challengeType: 0
dashedName: step-7
---

# --description--

To view the output of your `getMatchedWordCount` function in its current state, add a `console.log` statement below it. Inside the `log`, call the function with the argument `["I", "really", "really", "really", "like", "to", "code"]` for the `sentence` parameter and `"really"` for the `match` parameter.

# --hints--

Test 1

```js

```

# --seed--

## --seed-contents--

```js
function printCharacters(str) {
  for (const char of str) {
    console.log(char);
  }
}
printCharacters("hello");

function getMatchedWordCount(sentence, match) {
  let count = 0;
  return count;
}

--fcc-editable-region--

--fcc-editable-region--
```
