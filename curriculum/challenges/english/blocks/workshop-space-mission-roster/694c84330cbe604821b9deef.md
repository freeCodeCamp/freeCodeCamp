---
id: 694c84330cbe604821b9deef
title: Step 3
challengeType: 1
dashedName: step-3
---

# --description--

Mission control requires a method for adding new members to the roster. Create an empty `addCrewMember()` function that accepts two parameters, `crew` and `astronaut`.

# --hints--

You should create an `addCrewMember()` function.

```js
assert.isFunction(addCrewMember);
```

Your `addCrewMember()` function should have parameters `crew` and `astronaut`.

```js
const regex = __helpers.functionRegex('addCrewMember', ['crew', 'astronaut']);
assert.match(__helpers.removeJSComments(addCrewMember.toString()), regex);
```

# --seed--

## --seed-contents--

```js
const squad = [];

const firstAstronaut = {
  id: 1,
  name: "Andy",
  role: "Commander",
  isEVAEligible: true,
  priority: 3
};

--fcc-editable-region--

--fcc-editable-region--
```
