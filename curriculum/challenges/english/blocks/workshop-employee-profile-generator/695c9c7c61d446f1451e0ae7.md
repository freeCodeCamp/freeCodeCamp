---
id: 695c9c7c61d446f1451e0ae7
title: Step 4
challengeType: 20
dashedName: step-4
---

# --description--

Let us now create a variable to store the Employee's address. Create a variable `address` and store `123 Main Street` in it.

# --hints--

You should use the `=` operator to add `'123 Main Street'` to the `address` variable.

```js
({ test: () => true })
```

You should have the correct string in `address`.

```js
({ test: () => true })
```

# --seed--

## --seed-contents--

```python
first_name = 'John'
last_name = 'Doe'
full_name = first_name + ' ' + last_name

--fcc-editable-region--

--fcc-editable-region--
```

