---
id: 695df6480de23758af5a29cd
title: Step 7
challengeType: 20
dashedName: step-7
---

# --description--

We want to create a string that displays the employee's age.


Create a variable `employee_age` and assign it the number `28`.
Then, create a variable `employee_info` and assign it the result of concatenating `full_name`, the string `' is '`, `employee_age`, and the string `' years old'`.

Finally, print `employee_info`.


**Note:** You might see an error in the console. This is called `TypeError`.

# --hints--

You should create a variable `employee_age` with the value `28`.

```js
({ test: () => true })
```

You should create a variable `employee_info` with the concatenation of `full_name`, the string `' is '`, `employee_age`, and the string `' years old'`.

```js
({ test: () => true })
```

# --seed--

## --seed-contents--

```python
first_name = 'John'
last_name = 'Doe'
print(first_name)
print(last_name)
full_name = first_name + ' ' + last_name
address = '123 Main Street'
address += ', Apartment 4B'
print(address)

--fcc-editable-region--

--fcc-editable-region--
```
