---
id: 695c96f82af4fb78e59fa9ed
title: Step 1
challengeType: 20
dashedName: step-1
---

# --description--

In Python, strings are used to store text data. You can create a string by enclosing text inside single quotes (`'`).


For example:

```python
greeting = 'Hello'
print(greeting) # Output: Hello
```

Create two variables `first_name` which stores 'John' and `last_name` which stores 'Doe'. Then print `first_name` and `last_name`.

# --hints--

You should create two variables `first_name` and `last_name`.

```js
({ test: () => true })
```

You should print the two variables.

```js
({ test: () => true })
```

# --seed--

## --seed-contents--

```python
--fcc-editable-region--

--fcc-editable-region--
```
