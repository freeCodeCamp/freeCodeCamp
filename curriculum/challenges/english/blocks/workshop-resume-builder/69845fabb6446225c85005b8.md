---
id: 69845fabb6446225c85005b8
title: Step 6
challengeType: 0
dashedName: step-6
---

# --description--

Add a `Skill` interface with the following properties:

- `id` of type `string`
- `name` of type `string`

# --hints--

You should create an interface named `Skill`.

```js

```

Your `Skill` interface should have an `id` property of type `string`.

```js

```

Your `Skill` interface should have a `name` property of type `string`.

```js

```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Resume Builder</title>
    <link rel="stylesheet" href="styles.css" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.0/css/all.min.css"
      integrity="sha512-DxV+EoADOkOygM4IR9yXP8Sb2qwgidEmeqAEmDKIOfPRQZOWbXCzLC6vjbZyy0vPisbH2SyW27+ddLVCN+OMzQ=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />
  </head>
  <body>
    <header>
      <div class="header-container">
        <div class="header-content">
          <div class="header-logo">
            <i class="fa-solid fa-file"></i>
            <h1 class="header-title">Resume Builder</h1>
          </div>
          <div class="header-actions">
            <button id="preview-toggle" class="preview-toggle">
              <i class="fa-solid fa-eye"></i>
              <span>Show Preview</span>
            </button>
          </div>
        </div>
      </div>
    </header>

    <main>
      <div class="main-grid">
        <div id="form-section" class="form-section">
          <div class="section-card">
            <h2 class="section-title">
              <i class="fa-solid fa-user"></i>
              Personal Information
            </h2>
            <div class="form-grid form-grid-2col">
              <div class="form-group full-width">
                <label class="form-label" for="full-name">Full Name *</label>
                <input type="text" id="full-name" class="form-input" placeholder="John Doe" />
              </div>
              <div class="form-group">
                <label class="form-label" for="email">
                  <i class="fa-solid fa-envelope"></i> Email *
                </label>
                <input type="email" id="email" class="form-input" placeholder="john@example.com" />
              </div>
              <div class="form-group">
                <label class="form-label" for="phone">
                  <i class="fa-solid fa-phone"></i> Phone *
                </label>
                <input type="tel" id="phone" class="form-input" placeholder="+1 (555) 123-4567" />
              </div>
              <div class="form-group">
                <label class="form-label" for="location">
                  <i class="fa-solid fa-location-dot"></i> Location *
                </label>
                <input type="text" id="location" class="form-input" placeholder="New York, NY" />
              </div>
              <div class="form-group">
                <label class="form-label" for="website">
                  <i class="fa-solid fa-globe"></i> Website
                </label>
                <input type="url" id="website" class="form-input" placeholder="https://johndoe.com" />
              </div>
              <div class="form-group">
                <label class="form-label" for="linkedin">
                  <i class="fa-brands fa-linkedin"></i> LinkedIn
                </label>
                <input type="url" id="linkedin" class="form-input" placeholder="https://linkedin.com/in/johndoe" />
              </div>
              <div class="form-group full-width">
                <label class="form-label" for="summary">Professional Summary *</label>
                <textarea id="summary" class="form-textarea" rows="4" placeholder="A brief summary of your professional background and career objectives..."></textarea>
              </div>
            </div>
          </div>

          <div class="section-card">
            <div class="section-header">
              <h2 class="section-title">
                <i class="fa-solid fa-briefcase"></i> Work Experience
              </h2>
              <button id="add-experience" class="btn-primary">
                <i class="fa-solid fa-plus"></i> Add Experience
              </button>
            </div>
            <div id="experience-list" class="item-list"></div>
            <div id="experience-empty" class="empty-state">
              <i class="fa-solid fa-briefcase"></i>
              <p>No work experience added yet.</p>
              <p>Click "Add Experience" to get started.</p>
            </div>
          </div>

          <div class="section-card">
            <div class="section-header">
              <h2 class="section-title">
                <i class="fa-solid fa-graduation-cap"></i> Education
              </h2>
              <button id="add-education" class="btn-primary">
                <i class="fa-solid fa-plus"></i> Add Education
              </button>
            </div>
            <div id="education-list" class="item-list"></div>
            <div id="education-empty" class="empty-state">
              <i class="fa-solid fa-graduation-cap"></i>
              <p>No education added yet.</p>
              <p>Click "Add Education" to get started.</p>
            </div>
          </div>

          <div class="section-card">
            <div class="section-header">
              <h2 class="section-title">
                <i class="fa-solid fa-bolt"></i> Skills
              </h2>
              <button id="add-skill" class="btn-primary">
                <i class="fa-solid fa-plus"></i> Add Skill
              </button>
            </div>
            <div id="skills-list" class="skills-grid"></div>
            <div id="skills-empty" class="empty-state">
              <i class="fa-solid fa-bolt"></i>
              <p>No skills added yet.</p>
              <p>Click "Add Skill" to get started.</p>
            </div>
          </div>
        </div>

        <article id="preview-section" class="preview-section">
          <div class="preview-sticky">
            <div class="preview-info">
              <h2 class="preview-title">Live Preview</h2>
              <p class="preview-description">Your resume updates in real-time as you fill out the form.</p>
            </div>
            <div class="preview-container">
              <div class="resume-preview">
                <div class="resume-header">
                  <div class="resume-header-content">
                    <h1 id="preview-name" class="resume-name">Your Name</h1>
                    <div id="preview-contact" class="resume-contact"></div>
                  </div>
                </div>
                <div class="resume-body">
                  <section id="preview-summary-section" class="resume-section" style="display: none;">
                    <h2 class="resume-section-title">Professional Summary</h2>
                    <p id="preview-summary" class="resume-summary"></p>
                  </section>
                  <section id="preview-experience-section" class="resume-section" style="display: none;">
                    <h2 class="resume-section-title">Work Experience</h2>
                    <div id="preview-experience" class="resume-items"></div>
                  </section>
                  <section id="preview-education-section" class="resume-section" style="display: none;">
                    <h2 class="resume-section-title">Education</h2>
                    <div id="preview-education" class="resume-items"></div>
                  </section>
                  <section id="preview-skills-section" class="resume-section" style="display: none;">
                    <h2 class="resume-section-title">Skills</h2>
                    <div id="preview-skills" class="resume-skills-grid"></div>
                  </section>
                </div>
              </div>
            </div>
          </div>
        </article>
      </div>
    </main>

    <script src="script.ts" type="module"></script>
  </body>
</html>
```

```css
:root {
  --primary-blue: #2563eb;
  --primary-blue-dark: #1d4ed8;
  --slate-50: #f8fafc;
  --slate-100: #f1f5f9;
  --slate-200: #e2e8f0;
  --slate-300: #cbd5e1;
  --slate-500: #64748b;
  --slate-600: #475569;
  --slate-700: #334155;
  --slate-800: #1e293b;
  --slate-900: #0f172a;
  --red-500: #ef4444;
  --red-700: #b91c1c;
  --green-600: #16a34a;
  --blue-100: #dbeafe;
  --border-radius: 0.5rem;
  --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
  --shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
}

* {
  box-sizing: border-box;
}

html {
  font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  height: 100%;
  margin: 0;
  padding: 0;
}

body {
  margin: 0;
  padding: 0;
  min-height: 100vh;
  background-color: var(--slate-50);
}

header {
  background-color: white;
  box-shadow: var(--shadow-sm);
  border-bottom: 1px solid var(--slate-200);
}

.header-container {
  max-width: 80rem;
  margin: 0 auto;
  padding: 0 1rem;
}

.header-content {
  display: flex;
  justify-content: space-between;
  align-items: center;
  height: 4rem;
}

.header-logo {
  display: flex;
  align-items: center;
}

.header-logo i {
  margin-right: 0.25rem;
  font-size: 30px;
  color: var(--primary-blue);
}

.header-title {
  font-size: 1.5rem;
  font-weight: bold;
  color: var(--slate-900);
  margin: 0;
}

.header-actions {
  display: flex;
  align-items: center;
  gap: 1rem;
}

.preview-toggle {
  display: flex;
  align-items: center;
  padding: 0.5rem 1rem;
  color: var(--slate-600);
  background: none;
  border: none;
  cursor: pointer;
  transition: color 0.2s;
  font-size: 1rem;
}

.preview-toggle:hover {
  color: var(--slate-900);
}

.preview-toggle i {
  margin-right: 0.5rem;
}

main {
  max-width: 80rem;
  margin: 0 auto;
  padding: 2rem 1rem;
}

.main-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 2rem;
}

.form-section {
  display: flex;
  flex-direction: column;
  gap: 2rem;
}

.section-card {
  background-color: white;
  border-radius: var(--border-radius);
  box-shadow: var(--shadow-sm);
  border: 1px solid var(--slate-200);
  padding: 1.5rem;
}

.section-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 1.5rem;
}

.section-title {
  font-size: 1.25rem;
  font-weight: 600;
  color: var(--slate-800);
  display: flex;
  align-items: center;
  margin: 0;
}

.section-title i {
  margin-right: 0.5rem;
  font-size: 16px;
  color: var(--primary-blue);
}

.btn-primary {
  display: flex;
  align-items: center;
  padding: 0.5rem 1rem;
  background-color: var(--primary-blue);
  color: white;
  border: none;
  border-radius: var(--border-radius);
  cursor: pointer;
  transition: background-color 0.2s;
  font-size: 1rem;
}

.btn-primary:hover {
  background-color: var(--primary-blue-dark);
}

.btn-primary i {
  margin-right: 0.25rem;
  font-size: 12px;
}

.btn-delete {
  background: none;
  border: none;
  color: var(--red-500);
  cursor: pointer;
  transition: color 0.2s;
  padding: 0.5rem;
}

.btn-delete:hover {
  color: var(--red-700);
}

.btn-delete i {
  font-size: 16px;
}

.form-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 1rem;
}

.form-grid-2col {
  grid-template-columns: repeat(2, 1fr);
}

.form-group {
  display: flex;
  flex-direction: column;
}

.form-group.full-width {
  grid-column: 1 / -1;
}

.form-label {
  display: block;
  font-size: 0.875rem;
  font-weight: 500;
  color: var(--slate-700);
  margin-bottom: 0.5rem;
}

.form-label i {
  margin-right: 0.25rem;
  font-size: 16px;
}

.form-input,
.form-textarea {
  width: 100%;
  padding: 0.5rem 0.75rem;
  border: 1px solid var(--slate-300);
  border-radius: var(--border-radius);
  font-size: 1rem;
  transition: border-color 0.2s, box-shadow 0.2s;
}

.form-input:focus,
.form-textarea:focus {
  outline: none;
  border-color: var(--primary-blue);
  box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.1);
}

.form-input:disabled {
  background-color: var(--slate-100);
  cursor: not-allowed;
}

.form-textarea {
  resize: none;
}

.checkbox-wrapper {
  display: flex;
  align-items: center;
  margin-top: 0.5rem;
}

.checkbox-input {
  width: 1rem;
  height: 1rem;
  cursor: pointer;
}

.checkbox-label {
  margin-left: 0.5rem;
  font-size: 0.875rem;
  color: var(--slate-700);
  cursor: pointer;
}

.item-list {
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
}

.item-card {
  border: 1px solid var(--slate-200);
  border-radius: var(--border-radius);
  padding: 1rem;
  position: relative;
}

.item-card .btn-delete {
  position: absolute;
  top: 1rem;
  right: 1rem;
}

.item-fields {
  display: grid;
  grid-template-columns: 1fr;
  gap: 1rem;
  margin-bottom: 1rem;
}

.empty-state {
  text-align: center;
  padding: 2rem;
  color: var(--slate-500);
}

.empty-state i {
  font-size: 40px;
  margin-bottom: 1rem;
}

.empty-state p {
  margin: 0.5rem 0;
}

.skills-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 1rem;
}

.skill-card {
  border: 1px solid var(--slate-200);
  border-radius: var(--border-radius);
  padding: 1rem;
  position: relative;
}

.skill-card .btn-delete {
  position: absolute;
  top: 0.75rem;
  right: 0.75rem;
}

.preview-section {
  display: flex;
  flex-direction: column;
}

.preview-sticky {
  position: sticky;
  top: 2rem;
}

.preview-info {
  background-color: white;
  border-radius: var(--border-radius);
  box-shadow: var(--shadow-sm);
  border: 1px solid var(--slate-200);
  padding: 1.5rem;
  margin-bottom: 1.5rem;
}

.preview-title {
  font-size: 1.125rem;
  font-weight: 600;
  color: var(--slate-800);
  margin: 0 0 0.5rem 0;
}

.preview-description {
  font-size: 0.875rem;
  color: var(--slate-600);
  margin: 0;
}

.preview-container {
  background-color: var(--slate-100);
  border-radius: var(--border-radius);
  padding: 1rem;
  max-height: calc(100vh - 12rem);
  overflow: auto;
}

.resume-preview {
  background-color: white;
  box-shadow: var(--shadow-lg);
  border-radius: var(--border-radius);
  overflow: hidden;
  max-width: 56rem;
  margin: 0 auto;
}

.resume-header {
  background: linear-gradient(to right, var(--primary-blue), var(--primary-blue-dark));
  color: white;
  padding: 2rem;
}

.resume-header-content {
  text-align: center;
}

.resume-name {
  font-size: 2.25rem;
  font-weight: bold;
  margin: 0 0 0.5rem 0;
}

.resume-contact {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 1rem;
  color: var(--blue-100);
  font-size: 0.875rem;
  margin-top: 1rem;
}

.resume-contact-item {
  display: flex;
  align-items: center;
}

.resume-contact-item i {
  margin-right: 0.25rem;
  font-size: 15px;
  color: white;
}

.resume-body {
  padding: 2rem;
}

.resume-section {
  margin-bottom: 2rem;
}

.resume-section:last-child {
  margin-bottom: 0;
}

.resume-section-title {
  font-size: 1.5rem;
  font-weight: bold;
  color: var(--slate-800);
  border-bottom: 2px solid var(--primary-blue);
  padding-bottom: 0.5rem;
  margin: 0 0 1rem 0;
}

.resume-summary {
  color: var(--slate-700);
  line-height: 1.6;
}

.resume-items {
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
}

.resume-item {
  position: relative;
  padding-left: 1rem;
  border-left: 2px solid var(--slate-200);
}

.resume-item::before {
  content: '';
  position: absolute;
  width: 0.75rem;
  height: 0.75rem;
  background-color: var(--primary-blue);
  border-radius: 50%;
  left: -0.4375rem;
  top: 0.25rem;
}

.resume-item.education::before {
  background-color: var(--green-600);
}

.resume-item-header {
  display: flex;
  flex-direction: column;
  margin-bottom: 0.5rem;
}

.resume-item-position {
  font-size: 1.25rem;
  font-weight: 600;
  color: var(--slate-800);
  margin: 0;
}

.resume-item-company {
  font-size: 1.125rem;
  color: var(--primary-blue);
  font-weight: 500;
  margin: 0;
}

.resume-item-company.education {
  color: var(--green-600);
}

.resume-item-date {
  display: flex;
  align-items: center;
  color: var(--slate-600);
  font-size: 0.875rem;
  margin-top: 0.25rem;
}

.resume-item-date i {
  margin-right: 0.25rem;
  font-size: 16px;
}

.resume-item-description {
  color: var(--slate-700);
  line-height: 1.6;
  margin: 0;
}

.resume-item-gpa {
  color: var(--slate-600);
  margin: 0;
}

.resume-skills-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 1rem;
}

.resume-skill {
  font-weight: 500;
  color: var(--slate-700);
}

@media (min-width: 768px) {
  .form-grid-2col {
    grid-template-columns: repeat(2, 1fr);
  }

  .item-fields {
    grid-template-columns: repeat(2, 1fr);
  }

  .skills-grid {
    grid-template-columns: repeat(2, 1fr);
  }

  .resume-item-header {
    flex-direction: row;
    justify-content: space-between;
    align-items: flex-start;
  }

  .resume-item-date {
    margin-top: 0;
  }

  .resume-skills-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (min-width: 1024px) {
  .main-grid {
    grid-template-columns: repeat(2, 1fr);
  }

  .preview-toggle {
    display: none;
  }

  .form-section,
  .preview-section {
    display: block !important;
  }
}
```

```ts
interface PersonalInfo {
  fullName: string;
  email: string;
  phone: string;
  location: string;
  website?: string;
  linkedin?: string;
  summary: string;
}

interface Experience {
  id: string;
  company: string;
  position: string;
  startDate: string;
  endDate?: string;
  current: boolean;
  description: string;
}

interface Education {
  id: string;
  institution: string;
  degree: string;
  field: string;
  startDate: string;
  endDate: string;
  current: boolean;
  gpa?: string;
}

--fcc-editable-region--

--fcc-editable-region--
```
