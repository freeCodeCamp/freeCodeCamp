---
id: 6565c234de8cdf673c96bdf3
title: Step 54
challengeType: 20
dashedName: step-54
---

# --description--

It works, but there are several things you can improve. First of all, calling a function with 5 arguments is not the best.

Modify your function declaration to accept default arguments. Use `16` for the `length` and `1` for the other constraints.

# --hints--

Test 1

```js

```

# --seed--

## --seed-contents--

```py
import re
import secrets
import string

--fcc-editable-region--
def generate_password(length, nums, special_chars, uppercase, lowercase):
--fcc-editable-region--    
    # Define the possible characters for the password
    letters = string.ascii_letters
    digits = string.digits
    symbols = string.punctuation

    # Combine all characters
    all_characters = letters + digits + symbols

    while True:
        password = ''
        # Generate password
        for _ in range(length):
            password += secrets.choice(all_characters)
        
        constraints = [
            (nums, r'\d'),
            (special_chars, fr'[{symbols}]'),
            (uppercase, r'[A-Z]'),
            (lowercase, r'[a-z]')
        ]

        # Check constraints
        count = 0
        for constraint, pattern in constraints:
            if constraint <= len(re.findall(pattern, password)):
                count += 1
        
        if count == 4:
            break
    
    return password
    

new_password = generate_password(8, 1, 1, 1, 1)
print(new_password)

```
