---
id: 6564d75a923d21815caaa445
title: Step 33
challengeType: 20
dashedName: step-33
---

# --description--

Your `constraints` list is going to store tuples in which the first item is the constraint parameter — namely `nums`, `special_chars`, `uppercase`, and `lowercase`. The second item will be the regex pattern to search for inside the generated password.

Add a tuple to your list. Use `nums` as the first item and an empty string as the second item.

# --hints--

Test 1

```js

```

# --seed--

## --seed-contents--

```py
import re
import secrets
import string

def generate_password(length, nums, special_chars, uppercase, lowercase):
    
    # Define the possible characters for the password
    letters = string.ascii_letters
    digits = string.digits
    symbols = string.punctuation

    # Combine all characters
    all_characters = letters + digits + symbols

    while True:
        password = ''
        # Generate password
        for _ in range(length):
            password += secrets.choice(all_characters)
--fcc-editable-region--            
        
        constraints = [
            
        ]
        
--fcc-editable-region--
    
    return password
    

#new_password = generate_password(8)
#print(new_password)

```
