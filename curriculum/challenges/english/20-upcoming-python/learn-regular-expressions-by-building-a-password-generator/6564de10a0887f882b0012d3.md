---
id: 6564de10a0887f882b0012d3
title: Step 38
challengeType: 20
dashedName: step-38
---

# --description--

The `^` character at the beginning of a character class negates the pattern. For example: `[^A]` matches all characters except uppercase A.

Modify your empty raw string into a pattern that negates all lowercase letters.

# --hints--

Test 1

```js

```

# --seed--

## --seed-contents--

```py
import re
import secrets
import string

def generate_password(length, nums, special_chars, uppercase, lowercase):
    
    # Define the possible characters for the password
    letters = string.ascii_letters
    digits = string.digits
    symbols = string.punctuation

    # Combine all characters
    all_characters = letters + digits + symbols

    while True:
        password = ''
        # Generate password
        for _ in range(length):
            password += secrets.choice(all_characters)
--fcc-editable-region--
        
        constraints = [
            (nums, r'\d'),
            (special_chars, r'')
        ]
        
--fcc-editable-region--
    
    return password
    

#new_password = generate_password(8)
#print(new_password)

```
