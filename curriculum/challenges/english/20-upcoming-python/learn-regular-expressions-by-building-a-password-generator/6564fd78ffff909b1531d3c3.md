---
id: 6564fd78ffff909b1531d3c3
title: Step 47
challengeType: 20
dashedName: step-47
---

# --description--

The `findall()` function from the `re` module accepts two arguments: the first is the regex pattern to match and the second is string to analize. It returns all the matches inside the scanned string as a list.

Inside the `for` loop, call the `findall()` function passing `pattern` and `password` as the arguments.

# --hints--

Test 1

```js

```

# --seed--

## --seed-contents--

```py
import re
import secrets
import string

def generate_password(length, nums, special_chars, uppercase, lowercase):
    
    # Define the possible characters for the password
    letters = string.ascii_letters
    digits = string.digits
    symbols = string.punctuation

    # Combine all characters
    all_characters = letters + digits + symbols

    while True:
        password = ''
        # Generate password
        for _ in range(length):
            password += secrets.choice(all_characters)
        
        constraints = [
            (nums, r'\d'),
            (special_chars, fr'[{symbols}]'),
            (uppercase, r'[A-Z]'),
            (lowercase, r'[a-z]')
        ]
--fcc-editable-region--
        # Check constraints
        for constraint, pattern in constraints:
            
--fcc-editable-region--
    
    return password
    

#new_password = generate_password(8)
#print(new_password)

```
