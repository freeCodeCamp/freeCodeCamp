---
id: 6564f6bcaac826965125cb47
title: Step 44
challengeType: 20
dashedName: step-44
---

# --description--

Finally, add a fourth tuple with `lowercase` as the first item and the correspondent regex pattern as the second item.

# --hints--

Test 1

```js

```

# --seed--

## --seed-contents--

```py
import re
import secrets
import string

def generate_password(length, nums, special_chars, uppercase, lowercase):
    
    # Define the possible characters for the password
    letters = string.ascii_letters
    digits = string.digits
    symbols = string.punctuation

    # Combine all characters
    all_characters = letters + digits + symbols

    while True:
        password = ''
        # Generate password
        for _ in range(length):
            password += secrets.choice(all_characters)
--fcc-editable-region--
        
        constraints = [
            (nums, r'\d'),
            (special_chars, fr'[{symbols}]'),
            (uppercase, r'[A-Z]')
        ]
        
--fcc-editable-region--
    
    return password
    

#new_password = generate_password(8)
#print(new_password)

```
