---
id: 6564cf2e8642517abdf9d6e2
title: Step 27
challengeType: 20
dashedName: step-27
---

# --description--

Now, there's a match because the `[ac]` pattern means the matched character can be either `a` or `c`. Character classes also allow you to specify a character range by indicating the starting and the ending charaters separated by an hyphen.

Modify your pattern by adding a `-` in between `a` and `c`.

# --hints--

Test 1

```js

```

# --seed--

## --seed-contents--

```py
import re
import secrets
import string

def generate_password(length):
    
    # Define the possible characters for the password
    letters = string.ascii_letters
    digits = string.digits
    symbols = string.punctuation

    # Combine all characters
    all_characters = letters + digits + symbols

    password = ''
    # Generate password
    for _ in range(length):
        password += secrets.choice(all_characters)
        
    return password
    

new_password = generate_password(8)
print(new_password)
--fcc-editable-region--
pattern = re.compile('[ac]')
print(pattern.search('abc')
--fcc-editable-region--
```
