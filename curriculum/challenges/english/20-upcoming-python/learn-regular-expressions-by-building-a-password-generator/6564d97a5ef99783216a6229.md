---
id: 6564d97a5ef99783216a6229
title: Step 34
challengeType: 20
dashedName: step-34
---

# --description--

You want to match a single digit character out of all the possible digits. Modify your empty string into a regex pattern that makes use of a character class to match any digit from `0` to `9`.

# --hints--

Test 1

```js

```

# --seed--

## --seed-contents--

```py
import re
import secrets
import string

def generate_password(length, nums, special_chars, uppercase, lowercase):
    
    # Define the possible characters for the password
    letters = string.ascii_letters
    digits = string.digits
    symbols = string.punctuation

    # Combine all characters
    all_characters = letters + digits + symbols

    while True:
        password = ''
        # Generate password
        for _ in range(length):
            password += secrets.choice(all_characters)
--fcc-editable-region--
        
        constraints = [
            (nums, '')
        ]
        
--fcc-editable-region--
    
    return password
    

#new_password = generate_password(8)
#print(new_password)

```
