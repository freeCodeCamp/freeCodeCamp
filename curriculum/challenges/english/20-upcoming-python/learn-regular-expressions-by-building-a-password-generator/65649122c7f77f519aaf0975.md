---
id: 65649122c7f77f519aaf0975
title: Step 20
challengeType: 20
dashedName: step-20
---

# --description--

Finally, call your function with `8` as the argument and assign your function call to a `new_password` variable.

# --hints--

Test 1

```js

```

# --seed--

## --seed-contents--

```py
import secrets
import string
--fcc-editable-region--
def generate_password(length):
    
    # Define the possible characters for the password
    letters = string.ascii_letters
    digits = string.digits
    symbols = string.punctuation

    # Combine all characters
    all_characters = letters + digits + symbols

    password = ''
    # Generate password
    for _ in range(length):
        password += secrets.choice(all_characters)
        
    return password
    
--fcc-editable-region--
```
