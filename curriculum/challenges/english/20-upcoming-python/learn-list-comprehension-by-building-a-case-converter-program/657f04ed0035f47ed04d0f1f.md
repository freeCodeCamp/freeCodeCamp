---
id: 657f04ed0035f47ed04d0f1f
title: Step 13
challengeType: 20
dashedName: step-13
---

# --description--

So, if the `__name__` of the file is `__main__`, you can allow the `main()` function to run. So inside the body of the `if` statement, put a call to the `main()` function. This complete the script.

You can always expand on the ideas implemented in this code. Things like, making sure that the input string is not already in snake case format or the string does not have any invalid characters.

# --hints--

Test 1

```js

```

# --seed--

## --seed-contents--

```py
def convert_to_snake_case(pascal_or_camel_cased_string):
    snake_cased_char_list = []
    for char in pascal_or_camel_cased_string:
        if char.isupper():
            snake_cased_char_list.append(f'_{char.lower()}')
        else:
            snake_cased_char_list.append(char)
    snake_cased_string = ''.join(snake_cased_char_list)

    return snake_cased_string.strip('_')

def main():
    input_string = 'aLongAndComplexString'
    output_string = convert_to_snake_case(input_string)
    print(output_string)

if __name__ == '__main__':
    main()
--fcc-editable-region--


--fcc-editable-region--
```
