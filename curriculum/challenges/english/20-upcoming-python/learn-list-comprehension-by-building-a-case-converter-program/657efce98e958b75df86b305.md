---
id: 657efce98e958b75df86b305
title: Step 6
challengeType: 20
dashedName: step-6
---

# --description--

Characters that are already in lower case in the input string requires no special treatment whatsoever. You can directly append them to the list of snake case characters.

Create an `else` block on the same level as the `if` block by writing out the word `else` followed by a colon. On the next line or within the body of the `else` statement, append the current value of the target variable to list of snake case characters.

# --hints--

Test 1

```js

```

# --seed--

## --seed-contents--

```py
def convert_to_snake_case(pascal_or_camel_cased_string):
    snake_cased_char_list = []
    for char in pascal_or_camel_cased_string:
        if char.isupper():
            snake_cased_char_list.append(f'_{char.lower()}')
--fcc-editable-region--


--fcc-editable-region--
```
