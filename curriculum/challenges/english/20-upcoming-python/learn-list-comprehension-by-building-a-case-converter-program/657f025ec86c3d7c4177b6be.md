---
id: 657f025ec86c3d7c4177b6be
title: Step 11
challengeType: 20
dashedName: step-11
---

# --description--

Create another string variable for the output string named `output_string`. Put the equal sign or the assignment operator after the name of the variable, followed by a call to the `convert_to_snake_case()` function.

The `convert_to_snake_case()` accepts a string as its input parameter so pass the `input_string` variable here. Finally you can print out the value of the `output_string` variable using a call to the `print()` function.

# --hints--

Test 1

```js

```

# --seed--

## --seed-contents--

```py
def convert_to_snake_case(pascal_or_camel_cased_string):
    snake_cased_char_list = []
    for char in pascal_or_camel_cased_string:
        if char.isupper():
            snake_cased_char_list.append(f'_{char.lower()}')
        else:
            snake_cased_char_list.append(char)
    snake_cased_string = ''.join(snake_cased_char_list)

    return snake_cased_string.strip('_')

def main():
    input_string = 'aLongAndComplexString'
--fcc-editable-region--


--fcc-editable-region--
```
