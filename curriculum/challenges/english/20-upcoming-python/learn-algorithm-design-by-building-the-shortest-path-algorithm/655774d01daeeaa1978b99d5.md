---
id: 655774d01daeeaa1978b99d5
title: Step 14
challengeType: 20
dashedName: step-14
---

# --description--

At the beginning, all the other nodes in the graph are considered to be at infinite distance from the source node, because the distance has not determined yet.

Create an `else` statement and assign an infinite value to the node in the `distances` dictionary. For that you can use the `float()` function, that generates a floating point number from a number or a string. Pass the string `inf` to the `float()` function.

# --hints--

You should have an `else` statement.

```js

```

You should assign `float('inf')` to `distances[node]` inside your new `else` statement.

```js

```

# --seed--

## --seed-contents--

```py
my_graph = {
    'A': [('B', 5), ('C', 3), ('E', 11)],
    'B': [('A', 5), ('C', 1), ('F', 2)],
    'C': [('A', 3), ('B', 1), ('D', 1), ('E', 5)],
    'D': [('C',1 ), ('E', 9), ('F', 3)],
    'E': [('A', 11), ('C', 5), ('D', 9)],
    'F': [('B', 2), ('D', 3)]
}

--fcc-editable-region--
def shortest_path(graph, start):
    unvisited = []
    distances = {}
    for node in graph:
        unvisited.append(node)
        if node == start:
            distances[node] = 0
--fcc-editable-region--
```
