---
id: 656680b0fc79f2c38a34d90e
title: Step 6
challengeType: 0
dashedName: step-6
---

# --description--

It's time for us to sort and merge the lists (`left_part` and `right_part`) into the original `array`. 

We can do this by comparing elements on both lists, and merging the smaller element to the main list. We'll do this comparison for all the indexes in `left_part` and `right_part`.

First, let's create variables to help us keep track of each index as we sort them.

Create three variables: `left_array_index`, `right_array_index`, and `sorted_index`. Set their values to be 0.

# --hints--

Test 1

```js

```

# --seed--

## --seed-contents--

```py
def merge_sort(array):
    
    middle_point = len(array) // 2
    left_part = array[:middle_point]
    right_part = array[middle_point:]

    merge_sort(left_part)
    merge_sort(right_part)

--fcc-editable-region--

--fcc-editable-region--
```
