---
id: 656639771fed09b5c0e8fe71
title: Step 5
challengeType: 0
dashedName: step-5
---

# --description--

Now that we've divided the `array` list into two separate lists, we'll do the same thing for each list untile every element stands alone.

The idea is to keep dividing the array until all the elements stands alone as an individual unit. 

To do that, we have to recursively call the `merge_sort` function on the `left_part` and `right_part`.

Invoke the `merge_sort` function twice. Pass in `left_part` as a parameter to the first function call, and `right_part` to the second.

# --hints--

Test 1

```js

```

# --seed--

## --seed-contents--

```py
def merge_sort(array):
    
    middle_point = len(array) // 2
    left_part = array[:middle_point]
    right_part = array[middle_point:]
```

```py
--fcc-editable-region--

--fcc-editable-region--
```
