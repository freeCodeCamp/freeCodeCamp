---
id: 655a45c52fa3ea5ece3034c7
title: Step 31
challengeType: 20
dashedName: step-31
---

# --description--

Inside the `delete` method, call the private helper method `_delete` with the following parameters:

- `self.root`: This is the root of the binary search tree. The deletion operation starts from the root.
- `key`: This is the value that the user wants to delete from the BST.

# --hints--

Test 1

```js

```

# --seed--

## --seed-contents--

```py
class TreeNode:
    def __init__(self, key):
        self.key = key
        self.left = None
        self.right = None

class BinarySearchTree:
        def __init__(self):
            self.root = None

        def insert(self,key):
            self.root = self._insert(self.root, key)

        def _insert(self, node, key):
            if node is None:
                return TreeNode(key)
            if key < node.key:
                node.left = self._insert(node.left, key)
            elif key > node.key:
                node.right = self._insert(node.right, key)
            return node

        def search(self, key):
            return self._search(self.root, key)

        def _search(self, node, key):
            if node is None or node.key == key:
                return node
            if key < node.key:
                return self._search(node.left, key)
            return self._search(node.right, key)

        def delete(self, key):
--fcc-editable-region--
            pass
--fcc-editable-region--
```
