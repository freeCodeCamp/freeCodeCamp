---
id: 662fa7a2e834a7a3530db75b
title: Step 11
challengeType: 20
dashedName: step-11
---

# --description--

The `grade` method will return a number indicating the grade of the equation, which is the exponent of the highest `x` term. Therefore, within the `LinearEquation` class, return `1`.

# --hints--

Test 1

```js

```

# --seed--

## --seed-contents--

```py
--fcc-editable-region--
from abc import ABC, abstractmethod

class Equation(ABC):
    def __init__(self): pass
    
    @abstractmethod
    def grade(self): pass    
    
    @abstractmethod
    def solve(self): pass
        
    @abstractmethod
    def analyze(self): pass
        
class LinearEquation(Equation):
    def grade(self): pass
    
    def solve(self): pass
    
    def analyze(self): pass
    
lin_eq = LinearEquation()
--fcc-editable-region--
```
