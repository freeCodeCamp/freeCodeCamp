---
id: 62a23c1d505bfa13747c8a9b
title: Step 4
challengeType: 0
dashedName: step-4
---

# --description--

Wrap the numbers `0`, `100`, and `50` in `span` elements, and wrap those new `span` elements in `strong` elements. Then give your new `span` elements `id` values of `xpText`, `healthText`, and `goldText`, respectively.

# --hints--

you should add `span` element, inside your first `.stat` element.

```js

```

you should add `span` element, inside your second `.stat` element.

```js

```

you should add `span` element, inside your third `.stat` element.

```js

```

you should wrap `strong` element around your first newly created `span` element.

```js

```

you should wrap `strong` element around your second newly created `span` element.

```js

```

you should add `strong` element, inside your third `span` element.

```js

```

Your first `strong` element should wrap around text `0`.

```js

```

Your second `strong` element should wrap around text `100`.

```js

```

Your third `strong` element should wrap around text `50`.

```js

```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="./styles.css">
    <title>RPG - Dragon Repeller</title>
</head>
<body>
    <div id="game">
        <div id="stats">
--fcc-editable-region--
            <span class="stat">XP: 0</span>
            <span class="stat">Health: 100</span>
            <span class="stat">Gold: 50</span>
--fcc-editable-region--
        </div>
        <div id="controls"></div>
        <div id="monsterStats"></div>
        <div id="text"></div>
    </div>
</body>
```
