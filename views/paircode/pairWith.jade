script.
	var pairWithUser = !{JSON.stringify(pairWithUser)}
	var comment = !{JSON.stringify(comment)}
	var userGit = !{JSON.stringify(userGit)}

.spacer
	h2 Chatting with #{pairWithUser} about #{comment}
