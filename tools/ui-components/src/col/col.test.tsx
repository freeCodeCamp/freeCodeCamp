import React from 'react';
import { render } from '@testing-library/react';

import { Col } from '.';

describe('<Col />', () => {
  it('should render correctly', () => {
    render(<Col />);
  });
});
