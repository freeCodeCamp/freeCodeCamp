const pareto = require('./pareto');
const pr = require('./pr');
const search = require('./search');
const info = require('./info');
const boilerplates = require('./boilerplates');

module.exports = { pareto, pr, search, info, boilerplates };
