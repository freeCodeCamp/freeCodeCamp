---
title: Iterate with JavaScript While Loops
localeTitle: 在循环时使用JavaScript进行迭代
---
另一种类型的JavaScript循环称为`while loop`因为它`while`某些内容为true时运行，并在某些内容不再为true时停止。
```
var ourArray = []; 
 var i = 0; 
 while(i < 5) { 
  ourArray.push(i); 
  i++; 
 } 

```