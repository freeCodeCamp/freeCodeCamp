---
title: Composer
localeTitle: Compositor
---
## Compositor

Composer es un gestor de paquetes para paquetes PHP. Utiliza un archivo `composer.json` para configurar los paquetes para un proyecto PHP, similar al archivo `package.json` en los proyectos NodeJS.

### Instalar compositor

Para instalar Composer, primero debes descargarlo de [getcomposer.org](https://getcomposer.org/download/) .

A continuación, puede instalar Composer local o globalmente.

### Instalar paquetes

Instalar paquetes con la `composer install` . Composer instalará los paquetes listados en el archivo `composer.json` en el proveedor / carpeta.

```shell
    composer install 
```

### Más información:

*   El sitio web del compositor: [getcomposer.org](https://getcomposer.org/)
*   [Repositorio](https://github.com/composer/getcomposer.org) de GitHub del [compositor](https://github.com/composer/getcomposer.org) : [compositor / getcomposer](https://github.com/composer/getcomposer.org)
*   El popular repositorio de paquetes PHP que Composer usa para buscar paquetes: [Packagist](https://packagist.org/)