---
title: Textarea Tag
localeTitle: علامة Textarea
---
## علامة Textarea

تسمح لك علامة textarea HTML بإدخال مربع يحتوي على نص لملاحظات أو تفاعل المستخدم. في معظم الحالات ، من الشائع رؤية النص المستخدم كجزء من النموذج.

يستخدم المبرمجون tagarea tag لإنشاء حقل متعدد الأسطر لإدخال المستخدم (مفيد خصوصًا إذا كان المستخدم قادرًا على وضع نص أطول). قد يحدد المبرمجون سمات مختلفة للعلامات النصية.

عينة من الرموز:

 `
    <form> 
      <textarea name="comment" rows="8" cols="80" maxlength="500" placeholder="Enter your comment here..." required></textarea> 
    </form> 
` 

السمات الأكثر شيوعًا: تحدد `row` و `cols` ارتفاع وعرض النص تحدد السمة `placeholder` النص المرئي للمستخدم ، فهي تساعد المستخدم على فهم البيانات التي يجب كتابتها تحدد السمة `maxlength` الحد الأقصى لطول النص الذي يمكن كتابته في textarea ، لا يمكن للمستخدم إرسال المزيد من الأحرف تعني السمة `required` أنه يجب ملء هذا الحقل قبل إرسال النموذج

لمزيد من المعلومات حول علامة textarea وسماتها ، قم بزيارة MDN أو w3schools .