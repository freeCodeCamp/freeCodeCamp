---
title: is
localeTitle: é
---
## é

A palavra `is` chave `is` verifica se um objeto é compatível com um determinado tipo ou (iniciando com C # 7) testa uma expressão em relação a um padrão.

#### Mais Informações:

*   [C # Referência: é](https://docs.microsoft.com/dotnet/csharp/language-reference/keywords/is)