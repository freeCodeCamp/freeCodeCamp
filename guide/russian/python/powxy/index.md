---
title: Python Powxy
localeTitle: Python Powxy
---
`pow(x, y, z)` является встроенной в Python 3 функцией для возведения `x` в степень `y` . Если задано значение `z` , то функция возвращает `x` в степени `y` [деленное по модулю](https://processing.org/reference/modulo.html) `z` .

## Аргументы

Аргументы должны иметь числовые типы. 
Функция может принимать два аргумента: `x` и `y` , а также три: `x` , `y` и `z` . Если значение `z` задано, то значения `x` и `y` должны быть целочисленных типов, а значение `y` должно быть неотрицательным.

## Возвращаемое значение

Если `z` присутствует, функция возвращает `x` в степени `y`, деленное по модулю `z` . Если присутствуют только `x` и `y` , функция  возвращает `x` в степени `y` (то же, что и `x**y` ).

## Пример

```python
print(pow(2,4))    # выводит 16 
 print(pow(10,-2))  # выводит 0.01 
 print(pow(4,3,5))  # выводит 4 
```

[🚀Выполнить код](https://repl.it/CTGi)

[Официальная документация](https://docs.python.org/3/library/functions.html#pow)
